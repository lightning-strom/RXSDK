---
description: SDK 架构结构标准（多端通用）
alwaysApply: true
---

# SDK 架构标准

> **目标**：多渠道解耦、可插拔能力、核心不反向依赖渠道/厂商

---

## 分层架构

```
┌─────────────────────────────────────────┐
│           Demo / Host App               │  示例/宿主
├─────────────────────────────────────────┤
│         Channel Adapter                 │  渠道适配（薄）
├─────────────────────────────────────────┤
│         Capability Plugin               │  能力插件（登录/支付/分享等）
├─────────────────────────────────────────┤
│              Core                       │  核心层（网络/错误/回调/插件框架）
├─────────────────────────────────────────┤
│           Public API                    │  对外接口层
└─────────────────────────────────────────┘
```

---

## 层级职责

| 层级 | 职责 | 禁止事项 |
|------|------|----------|
| **Public API** | 暴露稳定接口与模型 | 禁止暴露 vendor 类型，禁止修改已发布签名 |
| **Core** | 通用能力与插件框架 | 禁止依赖渠道/插件，禁止硬编码渠道差异 |
| **Plugin** | 可插拔能力实现 | 禁止暴露 vendor 类型到 API 层 |
| **Adapter** | 装配插件、路由调用 | 禁止写复杂业务逻辑 |
| **Demo** | 集成示例 | 禁止迁回 SDK 核心作为依赖 |

---

## 依赖方向（硬约束）

```
Core ─────────→ 不能依赖任何渠道/插件/demo
Plugin ───────→ 只能依赖 Core（+ vendor SDK）
Adapter ──────→ 只能依赖 Core + 插件集合
```

---

## 统一错误结构

```json
{
  "code": 2000,
  "msg": "参数错误",
  "trace_id": "xxxx",
  "thirdcode": "WX_ERROR",
  "thirdmsg": "微信返回错误"
}
```

| 字段 | 说明 |
|------|------|
| `code` | 服务端 6 位 / 客户端 4 位 |
| `thirdcode/thirdmsg` | 三方原始错误，不混入 code |

---

## 能力路由

| 类型 | 命名规则 | 示例 |
|------|----------|------|
| route key | 全小写，稳定 | `wechat`, `facebook` |
| config key | 全大写，下划线 | `RX_PLUGIN_WECHAT` |

> 禁止用 config key 当作 route key

---

## Android 工程映射

| 层级 | 模块 |
|------|------|
| Core | `rxsdk_base` |
| UI | `rxsdk_base_ui` / `rxsdk_feedback_ui` |
| Adapter | `channel/*`、`overseas/*`、`push/*` |
| Plugin | 实现 `PluginSdk`，提供 `getInstance()` |

---

## 禁止事项

- ❌ Core 层依赖 vendor SDK / 渠道模块
- ❌ 插件层暴露 vendor 类型到 Public API
- ❌ 渠道间互相依赖
- ❌ 修改已发布接口签名

---

**最后更新**：2026-01-22  
**版本**：v3.0
