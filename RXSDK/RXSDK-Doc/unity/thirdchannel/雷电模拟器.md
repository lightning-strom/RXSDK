## SDK集成

<span style='color:red'>[官方文档](https://docs.ldmnq.com/docs/3iyTEo) </span>

在 mainTemplate.gradle 添加

```gradle
  implementation 'com.ruixue:rxsdk_ld:${version}'
```

## 配置说明

在 AndroidManifest.xml 添加

```xml
    <!--配置游戏渠道信息-->
    <meta-data android:name="GAME_ID" android:value="@your game_id"/>
    <meta-data android:name="CHANNEL_ID" android:value="@your channel_id"/>
    <meta-data android:name="SUN_CHANNEL_ID" android:value="@your sub_channel_id"/>
    注意：GAME_ID 如果和游戏有冲突，可改为LD_GAME_ID
```

## 初始化

```csharp
    private void Awake()
    {
        Dictionary<string, object> map = new();
        map.Add("ld_app_key", "@your ld_app_key");
        RuiXueSdk.InitThirdSdk(map, InitThirdSdkResponseDelegate, InitThirdSdkErrorDelegate);
        
    }
    
    public void InitThirdSdkResponseDelegate(string data)
    {
        LogUtil.Log("EventManager", $"RequestResponseDelegate: {data}");
    }
    
    public void InitThirdSdkErrorDelegate(string data)
    {
        LogUtil.Log("EventManager", $"RequestErrorDelegate: {data}");
    }
```

## 登录
请参考 [Unity端登录API](https://doc.ruixueyun.com/main/#/view?viewPath=deaadc5f-8b51-4f6c-8590-3c7a074cf1ad)

## 支付
请参考 [Unity端支付API](https://doc.ruixueyun.com/main/#/view?viewPath=708d6f55-e7f8-4c32-9aa7-d5beabfdcdee)

## 退出
请参考 [链接](https://doc.ruixueyun.com/main/#/view?viewPath=a1458347-d2f6-4570-8b27-0de8047b138b)

## 混淆说明

```
-keep class com.ld.sdk.** { *; }
-keep class com.changzhi.net.** { *; }
-dontwarn retrofit2.**
-keep class retrofit2.** { *; }
-keepattributes Signature
-keepattributes Exceptions
```