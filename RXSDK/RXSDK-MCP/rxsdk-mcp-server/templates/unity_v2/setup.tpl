# 瑞雪 SDK Unity v2 自动化接入
# Generated by MCP

# ==================== UPM 安装指令 ====================
setup_upm: |
  【你现在必须执行以下操作 - Unity v2 / UPM 方式】

  项目路径: {{.WorkspacePath}}
  SDK 版本: {{.Version}}

  1. 编辑 manifest.json
     - 文件路径: {{.WorkspacePath}}/Packages/manifest.json
     - 在 JSON 根对象中添加 scopedRegistries 配置（如已存在则追加）：
       ```json
       "scopedRegistries": [
         {
           "name": "RuiXueUnitySdk",
           "url": "http://60.205.123.114:4873",
           "scopes": ["com.ruixue"]
         }
       ]
       ```
     - 在 dependencies 中添加：
       ```json
       "com.ruixue.unitysdk": "{{.Version}}"
       ```

  2. 保存文件后 Unity 会自动下载并导入 SDK

  3. 验证安装
     - Unity 菜单：Window -> Package Manager
     - 确认 `com.ruixue.unitysdk` 已安装

  4. 完成后提醒用户：
     - 创建一个 MonoBehaviour 脚本用于 SDK 初始化
     - 调用 unity feature=init sdkApiVersion=v2 获取初始化代码（v2 推荐 `RXSDK.Initialize`）

# ==================== .unitypackage 安装指令 ====================
setup_unitypackage: |
  【兼容提示 - Unity v2 推荐 UPM，不建议 .unitypackage】

  项目路径: {{.WorkspacePath}}

  1. 说明当前 v2 主要以 UPM 包发布，不建议新项目继续使用 `.unitypackage`
  2. 若用户必须用 `.unitypackage`，提示其联系瑞雪技术支持获取对应 v2 版本包
  3. 推荐用户改用 installType=upm 重新执行 setup
