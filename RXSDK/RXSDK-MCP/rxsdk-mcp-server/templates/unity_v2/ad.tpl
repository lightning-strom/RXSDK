# 瑞雪 SDK Unity v2 广告
# Generated by MCP

# ==================== 激励广告 ====================
rewarded_video:
  method: "RXSDK.RewardedVideoAd(string adUnitId, bool isCheck, SdkCallback callback)"
  async: "RXAd.RewardedVideoAdAsync(string adUnitId, bool isCheck)"
  code: |
    using RuiXue;

    // 展示激励广告
    RXSDK.RewardedVideoAd("ad_unit_001", false, result =>
    {
        if (result.IsSuccess)
        {
            Debug.Log("激励广告完成: " + result.Data);
            // TODO: 发放奖励
        }
        else
        {
            Debug.LogError("激励广告失败: " + result.Error);
        }
    });

# ==================== Banner 广告 ====================
banner:
  method: "RXSDK.BannerAd(string adUnitId, Rect pos, float adIntervals, bool isCheck, SdkCallback callback)"
  code: |
    // 展示 Banner 广告
    RXSDK.BannerAd(
        "ad_unit_002",
        new Rect(0, 0, 320, 50),   // 位置和尺寸
        30f,                        // 轮播间隔（秒）
        false,
        result =>
        {
            if (result.IsSuccess) Debug.Log("Banner 广告展示成功");
        }
    );

# ==================== 插屏广告 ====================
interstitial:
  method: "RXSDK.InterstitialAd(string adUnitId, bool isCheck, SdkCallback callback)"
  code: |
    // 展示插屏广告
    RXSDK.InterstitialAd("ad_unit_003", false, result =>
    {
        if (result.IsSuccess) Debug.Log("插屏广告展示成功");
    });

# ==================== 导入语句 ====================
imports:
  - "using RuiXue;"
  - "using UnityEngine;"
