# 瑞雪 SDK Unity v2 实名认证
# Generated by MCP

# ==================== 方法签名 ====================
signature:
  method: "RXSDK.RealAuth(string realname, string idcard, SdkCallback callback)"
  async: "RXSDK.RealAuthAsync(string realname, string idcard)"

# ==================== 样板代码 ====================
code: |
  using RuiXue;

  // ========== 实名认证 ==========
  private void DoRealAuth(string realname, string idcard)
  {
      RXSDK.RealAuth(realname, idcard, result =>
      {
          if (result.IsSuccess)
          {
              Debug.Log("实名认证成功: " + result.Data);
          }
          else
          {
              Debug.LogError("实名认证失败: " + result.Error);
          }
      });
  }

  // ========== async 方式 ==========
  private async void DoRealAuthAsync()
  {
      var result = await RXSDK.RealAuthAsync("张三", "110101199001011234");
      if (result.IsSuccess) Debug.Log("实名认证成功");
  }

# ==================== 参数说明 ====================
parameters:
  - name: realname
    type: string
    description: 真实姓名
  - name: idcard
    type: string
    description: 身份证号码

# ==================== 导入语句 ====================
imports:
  - "using RuiXue;"
