# 瑞雪 Go 服务端 SDK 依赖接入
# Generated by MCP

placeholder_note:
  description: "@test 是占位符，接入时必须替换为真实业务参数。本模板版本号由参数 version 注入；未传时为 latest。"

overview:
  description: 通过 go modules 引入 github.com/ruixueyun/ruixuego
  module: github.com/ruixueyun/ruixuego

setup_steps:
  - step: 1
    title: 拉取依赖
    code: |
      go get github.com/ruixueyun/ruixuego@{{.Version}}

  - step: 2
    title: go.mod 示例片段
    code: |
      require (
      	github.com/ruixueyun/ruixuego {{.Version}}
      )

  - step: 3
    title: 整理依赖
    code: |
      go mod tidy

code_go: |
  // 在任意 Go 文件中：
  import "github.com/ruixueyun/ruixuego"

  // 然后执行：
  // go get github.com/ruixueyun/ruixuego@{{.Version}}
  // go mod tidy

notes:
  - 调用时可传 version=v0.1.57；不传则使用 latest
  - 私有仓库需配置 GOPROXY / GOPRIVATE（按公司规范）
  - 依赖添加后继续调用 ruixuego feature=init
  - 业务模块返回含 api_details（字段级说明 + 完整示例）；接入顺序见 feature=agent
  - 若仍看到字面量 @test，说明未走 dependency 模板渲染，请重新调用本工具
  - 推荐基准版本与文档对齐：v0.1.57
