/**
 * 瑞雪 SDK 初始化代码片段
 * Generated by MCP
 *
 * 按照以下步骤接入 SDK
 */

// ============================================================
// 步骤 1: Application 配置 (选择一种方式)
// ============================================================

// 方式一: 直接继承 RXApplication (推荐)
// -----------------------------------------
import com.ruixue.openapi.RXApplication;

public class GameApplication extends RXApplication {
    @Override
    public void onCreate() {
        super.onCreate();
        // 你的初始化代码...
    }
}

// 方式二: 间接调用 (已有自定义 Application)
// -----------------------------------------
import android.app.Application;
import android.content.Context;
import com.ruixue.openapi.RXSDK;

public class GameApplication extends Application {
    @Override
    public void onCreate() {
        super.onCreate();
        RXSDK.onApplicationCreate(this);
    }

    @Override
    protected void attachBaseContext(Context base) {
        super.attachBaseContext(base);
        RXSDK.attachBaseContext(base);
    }
}

// 方式三: 使用默认 Application (无自定义 Application)
// -----------------------------------------
// 在 AndroidManifest.xml 中配置:
// <application android:name="com.ruixue.openapi.RXApplication">
// </application>

// ============================================================
// 步骤 2: AndroidManifest.xml 注册 Application
// ============================================================

// <application android:name=".GameApplication">
// </application>

// ============================================================
// 步骤 3: Activity 生命周期监听 (选择一种方式)
// ============================================================

// 方式一: 自动监听 (AppCompatActivity，推荐)
// -----------------------------------------
import com.ruixue.openapi.RXSDK;

public class MainActivity extends AppCompatActivity {
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        // 自动监听生命周期
        RXSDK.trackingLifecycle(this);
    }
}

// 方式二: 手动调用 (继承 Activity)
// -----------------------------------------
import android.app.Activity;
import android.content.Intent;
import android.content.res.Configuration;
import com.ruixue.openapi.RXSDK;

public class GameActivity extends Activity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        RXSDK.onCreate(this);
    }

    @Override
    protected void onStart() {
        super.onStart();
        RXSDK.onStart(this);
    }

    @Override
    protected void onRestart() {
        super.onRestart();
        RXSDK.onRestart(this);
    }

    @Override
    protected void onResume() {
        super.onResume();
        RXSDK.onResume(this);
    }

    @Override
    protected void onPause() {
        super.onPause();
        RXSDK.onPause(this);
    }

    @Override
    protected void onStop() {
        super.onStop();
        RXSDK.onStop(this);
    }

    @Override
    protected void onNewIntent(Intent intent) {
        super.onNewIntent(intent);
        RXSDK.onNewIntent(this, intent);
    }

    @Override
    protected void onActivityResult(int requestCode, int resultCode, Intent data) {
        super.onActivityResult(requestCode, resultCode, data);
        RXSDK.onActivityResult(this, requestCode, resultCode, data);
    }

    @Override
    public void onConfigurationChanged(Configuration newConfig) {
        super.onConfigurationChanged(newConfig);
        RXSDK.onConfigurationChanged(this, newConfig);
    }

    @Override
    public void onRequestPermissionsResult(int requestCode, String[] permissions, int[] grantResults) {
        super.onRequestPermissionsResult(requestCode, permissions, grantResults);
        RXSDK.onRequestPermissionsResult(this, requestCode, permissions, grantResults);
    }
}

// ============================================================
// 步骤 4: SDK 初始化 (在主 Activity 的 onCreate 中)
// ============================================================

import com.ruixue.openapi.RXSDK;
import com.ruixue.RXSdkInitConfig;
import com.ruixue.RXRequestCallback;
import com.ruixue.logger.RXLogger;
import org.json.JSONObject;

import java.util.Arrays;
import java.util.List;

// 在 MainActivity 的 onCreate 中调用
private void initSdk() {
    // 必填参数
    String cpid = "your_cpid";              // CP 唯一 ID
    String productId = "your_product_id";   // 应用 ID
    String channelId = "your_channel_id";   // 渠道 ID
    List<String> baseUrlList = Arrays.asList(
            "https://api1.ruixueyun.com",
            "https://api2.ruixueyun.com"
    );

    // 创建初始化配置（使用构造函数）
    RXSdkInitConfig config = new RXSdkInitConfig(
            cpid,
            productId,
            channelId,
            baseUrlList,
            new RXRequestCallback() {
                @Override
                public void onResponse(JSONObject response) {
                    int code = response.optInt("code", -1);
                    if (code == 0) {
                        // 初始化成功，data 字段可能为空
                        JSONObject data = response.optJSONObject("data");
                        RXLogger.d("RXSDK 初始化成功");
                        // TODO: 初始化成功，可以进行登录等操作
                    } else {
                        // 初始化失败，读取 msg 字段
                        String msg = response.optString("msg", "未知错误");
                        RXLogger.e("RXSDK 初始化失败: " + msg);
                        // TODO: 处理初始化失败
                    }
                }
            }
    );

    // 可选配置
    config.setAutoInitThird(true);      // 是否自动初始化第三方 SDK
    config.setUsePrivacy(true);         // 是否展示隐私协议弹窗
    // config.setLogEnable(true);       // 日志开关
    // config.setAgreementTitle("用户协议和隐私政策");  // 协议标题
    // Map<String, String> agreementMap = new LinkedHashMap<>();
    // agreementMap.put("00001", "《用户协议》");
    // agreementMap.put("00002", "《隐私政策》");
    // config.setAgreementMap(agreementMap);

    // 初始化 SDK（推荐方式：传入 Activity）
    RXSDK.initialize(this, config);

    // 或者不传 Activity（需确保 config 中已设置 Activity）
    // RXSDK.initialize(config);
}

// ============================================================
// 步骤 5: 可选配置
// ============================================================

// 隐私协议配置 (在主 Activity 的 onCreate 中)
// -----------------------------------------
import com.ruixue.legal.PrivacyCallback;

RXSDK.getInstance().setPrivacyAgree(context, new PrivacyCallback() {
    @Override
    public void onPrivacyAgree(boolean userClick) {
        // 用户同意隐私协议后，初始化第三方 SDK
    }
});

