# 瑞雪 SDK 密码 UI 功能
# Generated by MCP
# 包含：找回密码、设置密码

# ==================== 找回密码弹窗 ====================
getBackPasswordWithComplete:
  method_name: "[[RXUIKitService sharedSDK] getBackPasswordWithComplete:]"
  description: 找回密码弹窗
  parameters:
    - name: complete
      type: void(^)(NSDictionary *response, RX_CommonRequestError *error)
      required: true
      description: 结果回调
  code: |
    // ========== 瑞雪 SDK 找回密码弹窗 ==========
    // 注意：需要添加 pod 依赖 pod 'RXUIKit'
    
    #import <RXUIKit/RXUIKitService.h>
    
    [[RXUIKitService sharedSDK] getBackPasswordWithComplete:^(NSDictionary *response, RX_CommonRequestError *error) {
        if (error) {
            NSLog(@"找回密码失败: %@", error.responesObject[@"msg"]);
            // TODO: 处理找回密码失败逻辑
        } else {
            NSLog(@"找回密码成功: %@", response);
            // TODO: 处理找回密码成功逻辑
        }
    }];

# ==================== 找回密码弹窗（扩展方式） ====================
getBackPasswordWithParams:
  method_name: "[[RXUIKitService sharedSDK] getBackPasswordWithParams:requestParams:complete:]"
  description: 找回密码弹窗（扩展方式，可配置参数）
  parameters:
    - name: params
      type: NSDictionary *
      required: true
      description: 页面配置信息
    - name: requestParams
      type: NSMutableDictionary *(^)(NSMutableDictionary *params)
      required: true
      description: 请求参数回调
    - name: complete
      type: void(^)(NSDictionary *response, RX_CommonRequestError *error)
      required: true
      description: 结果回调
  params_keys:
    - name: username
      type: NSString *
      description: 默认填充的账号
    - name: account_type
      type: NSNumber *
      description: 账号类型提示（1-通用提示，2-手机号提示，3-邮箱提示，默认 2）
    - name: password_hint
      type: NSString *
      description: 输入密码提示文本
  code: |
    // ========== 瑞雪 SDK 找回密码弹窗（扩展方式） ==========
    
    NSDictionary *params = @{
        @"username": @"xxxxxx",           // 默认填充的账号（可选）
        @"account_type": @2,              // 账号类型：1-通用，2-手机号，3-邮箱
        @"password_hint": @"请输入新密码"   // 密码输入提示（可选）
    };
    
    [[RXUIKitService sharedSDK] getBackPasswordWithParams:params
                                            requestParams:^NSMutableDictionary *(NSMutableDictionary *params) {
        // 可在此处理业务逻辑，返回参数控制 SDK 行为
        return params;
    } complete:^(NSDictionary *response, RX_CommonRequestError *error) {
        if (error) {
            NSLog(@"找回密码失败: %@", error.responesObject[@"msg"]);
        } else {
            NSLog(@"找回密码成功: %@", response);
        }
    }];

# ==================== 设置密码弹窗 ====================
setPasswordWithComplete:
  method_name: "[[RXUIKitService sharedSDK] setPasswordWithComplete:]"
  description: 设置密码弹窗
  parameters:
    - name: complete
      type: void(^)(NSDictionary *response, RX_CommonRequestError *error)
      required: true
      description: 结果回调
  code: |
    // ========== 瑞雪 SDK 设置密码弹窗 ==========
    // 注意：需要添加 pod 依赖 pod 'RXUIKit'
    
    #import <RXUIKit/RXUIKitService.h>
    
    [[RXUIKitService sharedSDK] setPasswordWithComplete:^(NSDictionary *response, RX_CommonRequestError *error) {
        if (error) {
            NSLog(@"设置密码失败: %@", error.responesObject[@"msg"]);
            // TODO: 处理设置密码失败逻辑
        } else {
            NSLog(@"设置密码成功: %@", response);
            // TODO: 处理设置密码成功逻辑
        }
    }];

# 导入语句
imports:
  - "#import <RXUIKit/RXUIKitService.h>"

# 依赖检查
dependency_check:
  ui_module:
    required: true
    pod_name: "RXUIKit"
    description: 密码 UI 需要添加此 pod 依赖

# AI 行为指南
check_guide: |
  插入密码 UI 代码前必须执行以下检查:
  1. 读取项目的 Podfile 文件
  2. 搜索是否存在 'RXUIKit' 依赖
  3. 如果不存在该依赖，自动添加
