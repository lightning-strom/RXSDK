# 瑞雪 SDK Unity v1 排行榜
# Generated by MCP

add_score:
  method: "RXRank.AddScore(string rank_id, int score, RequestResponseDelegate onResponse, RequestErrorDelegate onError)"
  code: |
    using RuiXue;
    using RuiXue.Rank;

    RXRank.AddScore("rank_001", 100, data => Debug.Log("增加分数成功"), error => Debug.LogError(error));

set_score:
  method: "RXRank.SetScore(string rank_id, int score, RequestResponseDelegate onResponse, RequestErrorDelegate onError)"
  code: |
    RXRank.SetScore("rank_001", 500, data => { }, error => Debug.LogError(error));

query_user_rank:
  method: "RXRank.QueryUserRank(string rank_id, string open_id, RequestResponseDelegate onResponse, RequestErrorDelegate onError)"
  code: |
    RXRank.QueryUserRank("rank_001", "user_open_id", data => Debug.Log("用户排名: " + data), error => Debug.LogError(error));

get_rank_list:
  method: "RXRank.GetRankList(string rank_id, int start_rank, int end_rank, RequestResponseDelegate onResponse, RequestErrorDelegate onError)"
  code: |
    RXRank.GetRankList("rank_001", 1, 100, data => Debug.Log("排行榜: " + data), error => Debug.LogError(error));

friends_rank:
  method: "RXRank.FriendsRank(string rank_id, RequestResponseDelegate onResponse, RequestErrorDelegate onError)"
  code: |
    RXRank.FriendsRank("rank_001", data => Debug.Log("好友排行榜: " + data), error => Debug.LogError(error));

imports:
  - "using RuiXue;"
  - "using RuiXue.Rank;"
