# 瑞雪 SDK Unity 依赖配置
# Generated by MCP

# ==================== 安装方式 ====================

# 方式一：Unity Package Manager (UPM) 安装（推荐）
install_upm: |
  ## UPM 安装步骤

  SDK 版本：{{.Version}}（未传入 version 时由 MCP 从 http://60.205.123.114:4873 获取 dist-tags.latest）

  ### 方法 A：通过 Scoped Registry（推荐）

  1. 打开 Unity 项目的 `Packages/manifest.json` 文件
  2. 在 `scopedRegistries` 中添加瑞雪 Registry：

  ```json
  {
    "scopedRegistries": [
      {
        "name": "RuiXue SDK",
        "url": "http://60.205.123.114:4873",
        "scopes": ["com.ruixue"]
      }
    ],
    "dependencies": {
{{.DefaultDependencies}}
    }
  }
  ```

  3. 保存后 Unity 会自动下载并导入 SDK

  ### 方法 B：通过本地路径（开发/离线使用）

  1. 将 SDK 包文件夹放到项目同级目录或任意位置
  2. 在 `Packages/manifest.json` 的 `dependencies` 中添加：

  ```json
  {
    "dependencies": {
      "com.ruixue.unitysdk.base": "file:../com.ruixue.unitysdk.base",
      "com.ruixue.unitysdk.share": "file:../com.ruixue.unitysdk.share",
      "com.ruixue.unitysdk.login": "file:../com.ruixue.unitysdk.login",
      "com.ruixue.unitysdk.pay": "file:../com.ruixue.unitysdk.pay"
    }
  }
  ```

  ### 方法 C：通过 Unity 编辑器

  1. 打开 Unity 编辑器
  2. 菜单栏：Window → Package Manager
  3. 点击左上角 "+" 按钮
  4. 选择 "Add package by name..."
  5. 分别输入：com.ruixue.unitysdk.base、com.ruixue.unitysdk.share、com.ruixue.unitysdk.login、com.ruixue.unitysdk.pay

# 方式二：.unitypackage 安装
install_unitypackage: |
  ## .unitypackage 安装步骤

  1. 获取 `RuiXueSDK.unitypackage` 文件
  2. 打开 Unity 编辑器
  3. 菜单栏：Assets → Import Package → Custom Package...
  4. 选择下载的 `.unitypackage` 文件
  5. 在弹出的导入窗口中勾选所有文件，点击 "Import"
  6. 等待导入完成

  **注意事项：**
  - 确保导入后 Assets 目录下出现 `RuiXue` 文件夹
  - 如有编译错误，检查 Unity 版本是否 >= 2019.4

# ==================== 环境要求 ====================
requirements: |
  - Unity 版本：2019.4 LTS 或更高
  - 目标平台：Android、iOS、WebGL
  - .NET 版本：.NET Standard 2.1 或 .NET Framework 4.x
  - Android 最低 API Level：21 (Android 5.0)
  - iOS 最低版本：11.0
  - Xcode：13.0+（iOS 构建时）

# ==================== Android 额外配置 ====================
android_config: |
  ## Android 平台配置

  1. Player Settings → Other Settings：
     - Minimum API Level: 21
     - Target API Level: 推荐 33+
     - Scripting Backend: IL2CPP（推荐）或 Mono

  2. 如需微信支付，在 `Assets/Plugins/Android/AndroidManifest.xml` 中配置：
     ```xml
     <activity
         android:name=".wxapi.WXPayEntryActivity"
         android:exported="true"
         android:launchMode="singleTop" />
     ```

# ==================== iOS 额外配置 ====================
ios_config: |
  ## iOS 平台配置

  1. Player Settings → Other Settings：
     - Target minimum iOS Version: 11.0
     - Architecture: ARM64
     - Scripting Backend: IL2CPP

  2. Xcode 工程配置（构建后自动处理或手动添加）：
     - 添加 URL Schemes（用于第三方登录回调）
     - 添加 Associated Domains（Universal Link）
     - Info.plist 隐私权限描述

# ==================== WebGL 小游戏包（微信 / 抖音）====================
minigame_packages: |
  ## 微信小游戏（WebGL）
  - 包名：com.ruixue.unitysdk.minigame.weixin（目录 com.ruixue.unitysdk.webgl.weixin）
  - 依赖：com.ruixue.unitysdk.base
  - 命名空间：RuiXue.MiniGame.WeiXin，入口类 RXMiniGameWeiXin
  - 专属能力：内容安全、客服、补单、开放数据域/云存储、分享、订阅消息、广告、公告/邮件、反馈、达人福利、数据上报
  - 运行时 JS：Plugins/WebGL/RXJsSdk_WeiXin/channelSDK/index.js（由 RXSDK-JS `npm run build:wegame` 基础包同步，禁用 wegame.full）

  ## 抖音小游戏（WebGL）
  - 包名：com.ruixue.unitysdk.minigame.douyin（目录 com.ruixue.unitysdk.webgl.douyin）
  - 依赖：com.ruixue.unitysdk.base、com.ruixue.unitysdk.share、com.ruixue.unitysdk.pay，并需集成 StarkSDK(TTSDK)
  - 命名空间：RuiXue.MiniGame.DouYin，入口类 RXMiniGameDouYin
  - 专属能力：抖音支付扩展、录屏、分享（视频/邀请）、客服、启动参数
  - 运行时 JS：Plugins/WebGL/RXJsSdk_DouYin/channel-sdk.douyin.v2.dev.umd.js（由 RXSDK-JS `npm run build:douyin` 的 dev umd 同步）

  说明：小游戏包只在 WebGL 平台生效，接口调用需包裹 #if UNITY_WEBGL。功能代码用 unity 工具 feature=minigame_weixin / minigame_douyin 获取。

# ==================== 注意事项 ====================
note: |
  1. UPM 方式和 .unitypackage 方式二选一，不要同时使用
  2. 升级 SDK 时，UPM 方式修改版本号即可；.unitypackage 方式需先删除旧文件再重新导入
  3. 默认接入 base、share、login、pay；后续按接口所属功能追加组件库，版本号与 base 保持一致
  4. SDK 基础包名为 com.ruixue.unitysdk.base，主要命名空间为 RuiXue
  5. 微信/抖音小游戏（WebGL）分别追加 com.ruixue.unitysdk.minigame.weixin / com.ruixue.unitysdk.minigame.douyin，并同步内嵌运行时 JS
