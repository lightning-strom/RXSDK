<?xml version="1.0" encoding="utf-8"?>
<!-- Gameplay Button - Green Gradient -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="112dp"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@drawable/bg_api_button"
    android:alpha="0.5"
    android:clickable="true"
    android:focusable="true">

    <!-- Icon Container -->
    <FrameLayout
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="@drawable/gradient_icon_green">

        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_gameplay"
            android:contentDescription="Gameplay" />
    </FrameLayout>

    <!-- Label -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/btn_gameplay"
        android:textSize="14sp"
        android:textStyle="bold"
        android:textColor="@color/primary_text"
        android:layout_marginTop="12dp"
        android:lineHeight="20dp" />

</LinearLayout>
