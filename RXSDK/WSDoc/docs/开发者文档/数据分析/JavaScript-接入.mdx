---
sidebar_position: 2
title: JavaScript 接入
description: "望舒SDK 数据分析 JavaScript 接入文档，提供详细的功能说明与接入指引。"
---

## 上报大数据

需要在 SDK 初始化后调用！！！


### JS

## 上报大数据

:::tip
需要在 SDK 初始化后调用！！！
:::


### JS

## 设置公共属性

设置后由 SDK 填入自定义属性中进行上报，设置后 SDK 进行缓存，多次设置则会覆盖。

### 响应结构

成功 code 为 0


### JS

## 修改公共属性

修改的属性会将原有属性覆盖，未设置的属性则会补入缓存中。

### 响应结构

成功 code 为 0


### JS

## 删除公共属性

删除缓存的某个公共属性。

### 响应结构

成功 code 为 0


## 设置公共属性

设置后由 SDK 填入自定义属性中进行上报，设置后 SDK 进行缓存，多次设置则会覆盖。

### 响应结构

成功 code 为 0

## 修改公共属性

修改的属性会将原有属性覆盖，未设置的属性则会补入缓存中。

### 响应结构

成功 code 为 0

## 删除公共属性

删除缓存的某个公共属性。

### 响应结构

成功 code 为 0

## 设置公共属性

设置后由 SDK 填入自定义属性中进行上报，设置后 SDK 进行缓存，多次设置则会覆盖。

### 响应结构

成功 code 为 0

## 修改公共属性

修改的属性会将原有属性覆盖，未设置的属性则会补入缓存中。

### 响应结构

成功 code 为 0

## 删除公共属性

删除缓存的某个公共属性。

### 响应结构

成功 code 为 0


## 设置公共属性

设置后由 SDK 填入自定义属性中进行上报，设置后 SDK 进行缓存，多次设置则会覆盖。

### 响应结构

成功 code 为 0

## 修改公共属性

修改的属性会将原有属性覆盖，未设置的属性则会补入缓存中。

### 响应结构

成功 code 为 0

## 删除公共属性

删除缓存的某个公共属性。

### 响应结构

成功 code 为 0


## 设置公共属性

设置后由 SDK 填入自定义属性中进行上报，设置后 SDK 进行缓存，多次设置则会覆盖。

### 响应结构

成功 code 为 0

## 修改公共属性

修改的属性会将原有属性覆盖，未设置的属性则会补入缓存中。

### 响应结构

成功 code 为 0

## 删除公共属性

删除缓存的某个公共属性。

### 响应结构

成功 code 为 0


### 响应结构

成功 code 为 0

### Unity

## 上报大数据

:::tip
需要在 SDK 初始化后调用！！！
:::


### Unity

## 设置公共属性

设置后由 SDK 填入自定义属性中进行上报，设置后 SDK 进行缓存，多次设置则会覆盖。

### 响应结构

成功 code 为 0


### Unity

## 修改公共属性

修改的属性会将原有属性覆盖，未设置的属性则会补入缓存中。

### 响应结构

成功 code 为 0


### Unity

## 删除公共属性

删除缓存的某个公共属性。

### 响应结构

成功 code 为 0


## 设置公共属性

设置后由 SDK 填入自定义属性中进行上报，设置后 SDK 进行缓存，多次设置则会覆盖。

### 响应结构

成功 code 为 0

## 修改公共属性

修改的属性会将原有属性覆盖，未设置的属性则会补入缓存中。

### 响应结构

成功 code 为 0

## 删除公共属性

删除缓存的某个公共属性。

### 响应结构

成功 code 为 0


## 设置公共属性

设置后由 SDK 填入自定义属性中进行上报，设置后 SDK 进行缓存，多次设置则会覆盖。

### 响应结构

成功 code 为 0

## 修改公共属性

修改的属性会将原有属性覆盖，未设置的属性则会补入缓存中。

### 响应结构

成功 code 为 0

## 删除公共属性

删除缓存的某个公共属性。

### 响应结构

成功 code 为 0


## 设置公共属性

设置后由 SDK 填入自定义属性中进行上报，设置后 SDK 进行缓存，多次设置则会覆盖。

### 响应结构

成功 code 为 0

## 修改公共属性

修改的属性会将原有属性覆盖，未设置的属性则会补入缓存中。

### 响应结构

成功 code 为 0

## 删除公共属性

删除缓存的某个公共属性。

### 响应结构

成功 code 为 0


## 设置公共属性

设置后由 SDK 填入自定义属性中进行上报，设置后 SDK 进行缓存，多次设置则会覆盖。

### 响应结构

成功 code 为 0

## 修改公共属性

修改的属性会将原有属性覆盖，未设置的属性则会补入缓存中。

### 响应结构

成功 code 为 0

## 删除公共属性

删除缓存的某个公共属性。

### 响应结构

成功 code 为 0

