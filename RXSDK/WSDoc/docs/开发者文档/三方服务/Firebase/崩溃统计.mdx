---
sidebar_position: 2
title: 崩溃统计
description: 望舒 崩溃统计 iOS / Android 接入文档，提供完整的接口调用说明与示例。
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<Tabs groupId="platform">

<TabItem value="ios" label="iOS">

请前往 [Firebase 官方平台](https://firebase.google.com/docs/crashlytics?hl=zh-cn) 查看配置。

:::info
Firebase Crashlytics 为第三方服务，SDK 仅提供基础集成，详细配置请参考官方文档。
:::

</TabItem>

<TabItem value="android" label="Android">

## SDK集成

项目创建以及全局 gradle 配置参照 Firebase 官方文档

[开始](https://firebase.google.com/docs/crashlytics/get-started?hl=zh-cn&platform=android)

[将 Firebase 添加到您的 Android 项目](https://firebase.google.com/docs/android/setup?hl=zh-cn#groovy)

官方 firebase-crashlytics SDK 无需引入，只需按以下方式接入SDK 即可。


:::tip[版本说明]
`version` 为 SDK 版本号（替换为实际版本号），最新版本请从 [更新日志](/docs/开发者文档/入门指南/更新日志/Android) 中查看。
:::

```groovy
//海外渠道引用此库
 implementation 'com.ruixue:rxsdk_firebase:${version}'
```

## 获取 Android NDK 崩溃报告

参考官网[获取 Android NDK 崩溃报告](https://firebase.google.com/docs/crashlytics/ndk-reports?hl=zh-cn)

## 使用 Google Analytics（分析）获取崩溃报告的指标

参考官网[使用 Google Analytics （分析）获取崩溃报告的指标](https://firebase.google.com/docs/crashlytics/start-using-analytics?hl=zh-cn&platform=android)

## 测试您的 Crashlytics 实现

参考官网[测试您的 Crashlytics 实现](https://firebase.google.com/docs/crashlytics/test-implementation?hl=zh-cn&platform=android)

## 在 Crashlytics 信息中心内获取易于理解的崩溃报告

参考官网[在 Crashlytics 信息中心内获取易于理解的崩溃报告](https://firebase.google.com/docs/crashlytics/get-deobfuscated-reports?hl=zh-cn&platform=android)

## 自定义 Firebase Crashlytics 崩溃报告

参考官网[自定义 Firebase Crashlytics 崩溃报告](https://firebase.google.com/docs/crashlytics/customize-crash-reports?hl=zh-cn&platform=android)

**添加自定义键**

自定义键可以帮助您获取导致崩溃的应用特定状态。 您可以将任意键值对与您的崩溃报告相关联，然后使用自定义键在 Firebase 控制台中搜索和过滤崩溃报告。

在 [Crashlytics](https://console.firebase.google.com/project/_/crashlytics?hl=zh-cn&_gl=1*1lfnutf*_ga*MTI3Njk0NTI3Ny4xNjkxMDUxMjYw*_ga_CW55HF8NVT*MTY5Mjc2NzgxOC4yNC4xLjE2OTI3Njg4MzMuMC4wLjA.) 信息中心内，您可以搜索与自定义键匹配的问题。

当您在控制台中查看某个特定问题时，可以查看每个事件关联的自定义键（在“键”子标签中），甚至可以按自定义键过滤事件（使用页面顶部的“过滤条件”菜单）。

注意： Crashlytics 最多可支持 64 个键值对。达到此阈值后，系统就不会再保存更多的值。每个键值对的大小上限为 1 kB。

使用 setCustomKey 实例方法设置键值对：请注意， setCustomKey 已重载，使得 value 形参可以接受任何基本实参或 String 实参。下面是一些示例：

**接口原型**

```java
    /**
    * 添加自定义键
    * @param key key
    * @param value value
    */
    public void setCustomKey(String key, String value);
```

**调用示例**

```java
    FirebaseSdkWrapper.getInstance().getFirebaseCrashlytics().setCustomKey("my_string_key", "foo");
```

通过将 CustomKeysAndValues 实例传递给 setCustomKeys 实例方法，批量添加键值对：

**接口原型**

```java
    /**
    * 批量添加键值对
    * @param keysAndValues 键值集合
    */
    public void setCustomKeys(RxCustomKeysAndValues keysAndValues);
```

**调用示例**

```java
    RxCustomKeysAndValues keysAndValues = new RxCustomKeysAndValues.Builder()
    .putString("string key", "string value")
    .putString("string key 2", "string value 2")
    .putBoolean("boolean key", true)
    .putBoolean("boolean key 2", false)
    .putFloat("float key", 1.01f)
    .putFloat("float key 2", 2.02f)
    .build();
    FirebaseSdkWrapper.getInstance().getFirebaseCrashlytics().setCustomKeys(keysAndValues);
```

**添加自定义日志消息**

为了了解导致崩溃的事件的更多背景信息，您可以向应用添加自定义 Crashlytics 日志。Crashlytics 会将日志与您的崩溃数据相关联，并将其显示在 [Firebase 控制台](https://console.firebase.google.com/project/_/crashlytics?hl=zh-cn&_gl=1*1oivyto*_ga*MTI3Njk0NTI3Ny4xNjkxMDUxMjYw*_ga_CW55HF8NVT*MTY5Mjc2NzgxOC4yNC4xLjE2OTI3Njg4MzMuMC4wLjA.)的 Crashlytics 页面的日志标签页下。

为了避免拖慢应用运行速度， Crashlytics 的日志大小限制为 64 kB。如果某个会话的日志量超过该限额， Crashlytics 就会删除较早的日志条目。

**接口原型**

```java
    /**
    * 添加自定义消息
    * @param message 消息体
    */
    public void log(String message);
```

**调用示例**

```java
    FirebaseSdkWrapper.getInstance().getFirebaseCrashlytics().log("message");
```

**设置用户标识符**

了解哪些用户遇到了特定的崩溃通常可以帮助您诊断问题。Crashlytics 提供了一种在崩溃报告中以匿名方式标识用户的方法。

如需将用户 ID 添加到报告中，请以 ID 编号、令牌或哈希值的形式为每个用户分配一个唯一标识符：

**接口原型**

```java
    /**
    * 设置用户标识
    * @param identifier 用户标识
    */
    public void setCrashUserId(String identifier);
```

**调用示例**

```java
    FirebaseSdkWrapper.getInstance().getFirebaseCrashlytics().setCrashUserId("user123456789");
```

如果您在设置某个用户标识符后需要将其清除，请将该值重置为空字符串。清除用户标识符不会移除现有的 Crashlytics 记录。如果您需要删除与用户 ID 关联的记录，[请与 Firebase 支持团队联系](https://firebase.google.com/support/troubleshooter/contact?hl=zh-cn)。

**报告非严重异常**

除了自动报告您应用中出现的崩溃， Crashlytics 还可帮您记录非严重异常，并在应用下次启动时将这些异常记录发送给您。

注意： Crashlytics 只会存储最近记录的 8 个异常。如果您的应用抛出的异常数超出了 8 个，则较早的异常会被舍弃。

使用 recordException 方法在您应用的 catch 块中记录非严重异常。例如：

**接口原型**

```java
    /**
    * 报告非常严重的错误
    * @param throwable 错误
    */
    public void recordException(Throwable throwable);
```

**调用示例**

```java
    try {
    throw new RuntimeException("Test Crash");
    } catch (Exception e) {
    FirebaseSdkWrapper.getInstance().getFirebaseCrashlytics().recordException(e);
    }
```

警告：如果您想在异常消息中包含唯一值（例如用户 ID 或时间戳），请使用自定义键，而不是直接在异常消息中添加值。直接添加值可能会导致一些问题，并可能导致 Crashlytics 在您的应用中对报告错误的功能进行限制。

所记录的所有异常在 Firebase 控制台中均显示为非严重问题。问题摘要中会包含您通常可以从崩溃中获得的所有状态信息，以及按 Android 版本和硬件设备细分的数据。

Crashlytics 在一个专用的后台线程中处理异常，以最大程度减少对您应用性能的影响。为了减少用户的网络流量， Crashlytics 会一并批量处理已记录的异常，并在应用下次启动时发送这些异常。

**启用自选式报告**

默认情况下， Crashlytics 会自动为应用的所有用户收集崩溃报告。为了让用户对其发送的数据有更多的控制权，您可以启用自选式报告功能，方法是停用自动报告功能，并且仅当您在代码中进行选择后向 Crashlytics 发送数据：

在 AndroidManifest.xml 文件的 application 部分中，添加 meta-data 标记以关闭自动收集功能：

```xml
meta-data
 android:name="firebase_crashlytics_collection_enabled"
 android:value="false" />
```

在运行时调用 Crashlytics 数据收集替换，从而为选定用户启用收集功能。在您应用的多次启动之间，替换值会保持不变，因此 Crashlytics 可以自动收集相关报告。如需停用自动崩溃报告功能，请将 false 作为替换值传递。将此设置为 false 时，新值要等到应用下次运行时才会生效。

**接口原型**

```java
    /**
    * 是否开启崩溃自动收集
    * @param enabled 是否开启
    */
    public void setCrashlyticsCollectionEnabled(boolean enabled);
```

**调用示例**

```java
    FirebaseSdkWrapper.getInstance().getFirebaseCrashlytics().setCrashlyticsCollectionEnabled(true);
```

注意：停用数据收集功能后， Crashlytics 会在设备本地存储崩溃信息。如果随后启用了数据收集功能，设备上存储的所有崩溃信息都将发送到 Crashlytics 进行处理。

## 将 Crashlytics 与 Google Play 集成

参考官网[将 Crashlytics 与 Google Play 集成](https://firebase.google.com/docs/crashlytics/integrate-with-google-play?hl=zh-cn)

## 在 Crashlytics 信息中心内根据 ANR 标记调试您的 Android 应用

参考官网[在 Crashlytics 信息中心内根据 ANR 标记调试您的 Android 应用](https://firebase.google.com/docs/crashlytics/debug-anr-errors?hl=zh-cn)

## 自定义疾速崩溃提醒

参考官网[自定义疾速崩溃提醒](https://firebase.google.com/docs/crashlytics/velocity-alerts?hl=zh-cn)

## 将数据导出到 BigQuery

参考官网[将数据导出到 BigQuery](https://firebase.google.com/docs/crashlytics/bigquery-export?hl=zh-cn)

## Crashlytics 问题排查和常见问题解答

参考官网[Crashlytics 问题排查和常见问题解答](https://firebase.google.com/docs/crashlytics/troubleshooting?hl=zh-cn&platform=android)

</TabItem>

</Tabs>

