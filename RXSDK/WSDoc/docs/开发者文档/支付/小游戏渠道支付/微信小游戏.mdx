---
sidebar_position: 16
title: 微信小游戏
description: "望舒SDK 支付 微信小游戏 小游戏 接入文档，提供完整的接口调用说明与示例。"
---
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

## 望舒支付配置

[虚拟支付2.0配置](/docs/开发者文档/三方服务/微信/小游戏配置/虚拟支付2-0支付配置说明)

[公众号支付配置](/docs/开发者文档/三方服务/微信/小游戏配置/公众号支付配置说明)

[直购模式支付配置](/docs/开发者文档/三方服务/微信/小游戏配置/直购模式支付配置说明)

[小游戏跳转支付配置](/docs/开发者文档/三方服务/微信/小游戏配置/小游戏跳转支付)

:::tip
补单机制：
由于app在上传凭证给服务器时发生了网络或各种原因，造成了凭证没有上传成功，产生了掉单，SDK 提供自动和客户端手动补单两种补偿机制。具体如下：

自动补单流程：

1. 客户端调用下单接口时 SDK 会优先判断上次支付是否掉单，如有掉单则先进行补单，补单成功后再次调用下单接口则恢复正常支付流程。补单失败后再次调用下单接口则继续补单。
2. 支付失败后和登录成功后SDK会自动进行5次补单，使用斐波那契数列5秒间隔计算，减少掉单率。

手动补单流程：

1. 订单验证失败 SDK 会将订单号及支付凭证回调给客户端，客户端可自行上报。
2. SDK 提供查询是否需要补单和补单功能，客户端可配合使用。建议在支付失败后和登录成功后调用。
   :::

## 下单支付

<Tabs groupId="platform">

<TabItem value="js" label="JS">

### 参数说明

| **字段** | **类型**      | **是否必填** | **备注**     |
| ------------ | ----------------- | ---------------- | ---------------- |
| params   | IPay          | 是           | 参数         |
| callback | IMethodParams | 是           | 回调函数集合 |

> IPay 说明

| **字段**       | **类型** | **是否必填** | **备注**           |
| ------------------ | ------------ | ---------------- | --------------------------------------------- |
| pay_type       | string   | 是           | 支付的类型 ==💡 如果之前从未接过腾讯 1.0 的米大师虚拟接口（wx.reqrequestMidasPayment）需要传递'minigame_v2'==            |
| goods_tag      | string   | 是           | 计费点标识       |
| trade_no       | string   | 是           | CP 订单号             |
| indulge_auth   | number   | 否           | 是否进行防沉迷支付验证 ，0 不验证 1 验证，默认不验证如果为 1 age 必传                                                  |
| age            | number   | 否           | 用户年龄indulge_auth 为 1 时必传该字段      |
| envVersion            | string   | 否           | 跳转小程序支付 跳转小程序版本  develop 开发版 trial 体验版 release 正式版 默认为release      |
| direct_send            | boolean   | 否           | 跳转客服是否直接推送消息卡片pay_type为 wxpub 时传该字段为true跳转客服直接推送消息卡片      |
| miniprogram_name            | string   | 否           | 小游戏名称pay_type为 jump_miniprogram 时必传该字段      |
| miniprogram_args            | number   | 否           | 透传参数      |
| preview_image            | boolean   | 否           | 是否预览小程序码 长按小程序码跳转小程序支付pay_type为 jump_miniprogram 时 填写 true 可预览小程序码 长按小程序码跳转小程序支付      |
| short_url            | string   | 否           | 短域名（3.9.36废弃）preview_image为 true时必传该字段       |
| title            | string   | 否           | 跳转客服时给用户发送卡片的标题当未传递此参数时，系统会取【微信客服自动推送】中关键字为ruixue_pay_wxpub_without_card的配置，当此配置未配置时不会发送卡片，所以请保证此配置存在。      |
| desc            | string   | 否           | 跳转客服时给用户发送卡片的描述当未传递此参数时，系统会取【微信客服自动推送】中关键字为ruixue_pay_wxpub_without_card的配置，当此配置未配置时不会发送卡片，所以请保证此配置存在。      |
| image            | string   | 否           | 跳转客服时给用户发送卡片的图片当未传递此参数时，系统会取【微信客服自动推送】中关键字为ruixue_pay_wxpub_without_card的配置，当此配置未配置时不会发送卡片，所以请保证此配置存在。       |
| age            | number   | 否           | 用户年龄indulge_auth 为 1 时必传该字段      |
| notify_url     | string   | 否           | 支付成功通知 CP 发货地址                |
| func           | string   | 否           | 埋点名称 当 wxpub 时 不传func字段的时候，跳转到客服系统时右下角不弹卡片，否则右下角弹出卡片               |
| transmit_args  | string   | 否           | 客户端透传参数，key1=val1&key2=val2 的格式    |
| zoneId         | string   | 否           | 分区 ，默认'1'仅在虚拟支付有效      |
| is_debug       | number   | 否           | 是否测试订单，0 正式 1 为测试订单，默认为 0       |
| env            | number   | 否           | 是否使用沙盒环境支付，0 正式 1 沙盒，默认为 0        |
| querystr       | string   | 否           | cp 方自定义，用于打开客服透传的 url 参数， 必须以&开头【custom_description】： 为 IOS 微信公众号支付自定义订单内容详情字段例如：querystr: '&custom_description=helloSDK'💡 SDK v3.2.0 版本开始**不支持**此字段，如需传客服透传参数请使用 sessionFromExt 字段 |
| sessionFromExt | object   | 否           | sessionFrom 扩展字段，cp 方自定义，用于打开客服透传的 sessionFrom 参数，💡 请使用最新版本 sdk 【例如】：`sessionFromExt: { custom_field: 'helloSDK' }`                                                                                                                 |
| game_info     | object    | 否           | 游戏自定义数据， 上报区服和角色需要在 game_info 中传入属性，具体使用方式参考下方 game_info 字段说明  |
| bigdata_report     | object     | 否           | 商业化支付参数， 通过商业化触发的支付行为要传以下参数，否则数据统计会有丢失，具体使用方式参考下方 bigdata_report 字段说明  |
| custom_ext         | object   | 否           |自定义透传参数 |

> `CustomExt` 字段说明

| **字段**  | **类型** | **是否必填** | **备注** |
| --------- | ------  | ----------- |---------|
| bigdata_ext| object| 否             | 大数据预置事件透传参数                          |

> `bigdata_report` 字段说明

| **字段**  | **类型** | **是否必填** | **备注** |
| --------- | ------  | ----------- |---------|
| scene_identifier| string| 否             | 场景标识                           |
| scene_name     | string| 否            | 场景名称                          |
| trigger_button_identifier     | string| 否            | 触发按钮标识                         |
| trigger_button_name     | string| 否            | 触发按钮名称                         |
| window_identifier     | string| 否            | 窗口标识                         |
| window_name     | string| 否            | 窗口名称                         |
| window_sequence     | string| 否            | 窗口展示顺序                         |
| gift_package     | object     | 否            | 礼包	                         |

> `gift_package` 字段说明

| **字段**  | **类型** | **是否必填** | **备注** |
| --------- | ------  | ----------- |---------|
| identifier| string| 否             | 礼包标识                           |
| name     | string| 否            | 礼包名称                          |
| purchase_medium     | string| 否            | 购买礼包介质                         |
| price     | number| 否            | 礼包价格                         |
| billing_point     | string| 否            | 礼包计费点                         |

> `game_info` 字段说明

| **字段**  | **类型** | **是否必填** | **备注** |
| --------- | ------  | ----------- |---------|
| cp_game_character_id| string| 否             | 角色 id                           |
| cp_game_area_id     | string| 否            | 区服 id                           |

> pay_type 说明

| **字段**             | **备注**                      |
| ------------------------ |---------------------------------|
| minigame/minigame_v2 | 米大师支付【支持 Android iOS】   |
| midas_game_item      | 小游戏道具直购【支持 Android iOS】                     |
| wxpub                | ios 跳转客服公众号支付               |
| jump_miniprogram                 | 跳转小程序支付                 |
| wechath5                 | 获取h5支付链接支付                 |
| aums                 | 银联云闪付 h5 支付                 |

> IMethodParams 说明

| **字段**        | **备注**                                                  |
| ------------------- | ------------------------------------------------------------- |
| complete        | (data) => voidcomplete：为函数且必传，data 为结果数据 |
| paySuccCallback | () => void支付成功后回调函数，选填                    |

**调用示例**
```javascript
sdk.pay(
        {
           'pay_type': 'minigame_v2',
           'goods_tag': 'bytest',
           'trade_no': '' + new Date().getTime(),
           'transmit_args': 'a=1&b=2',
           'indulge_auth': 1,
           'age':18,
           
           // 商业化下单参数示例
           // 'bigdata_report': {
           //   'scene_identifier': 'xxx',
           //   'scene_name': 'xxx',
           //   'trigger_button_identifier': 'xxx',
           //   'trigger_button_name': 'xxx',
           //   'window_identifier': 'xxx',
           //   'window_name': 'xxx',
           //   'window_sequence': 'xxx',
           //   'gift_package': {
           //     'identifier': 'xxx',
           //     'name': 'xxx',
           //     'purchase_medium': 'xxx',
           //     'price': 100,
           //     'billing_point': 'xxx',
           //   },
           // }
           
           // 游戏自定义数据示例
           // 'game_info': {
           //   'cp_game_character_id': 'test',
           //   'cp_game_area_id': 'testarea',
           // }
           
           // 大数据预置事件透传参数示例
           // 'custom_ext': {
           //   'bigdata_ext': { 'test': 'test' },
           // }
        },
        {
           complete: (data) => {
              console.log(data)
           }
        }
)

// 兑换道具
sdk.pay(
        {
          "exchange": true,
          "mode": 0,
          "scene_tag": "string",
          "window_id": 0,
          "window_version": "string",
          "cp_gift_tag": "string",
          "cp_prop_tag": "string",
          "cp_prop_number": 0,
          "item_list": [
            {
              "number": 0,
              "tag": "string"
            }
          ]
        },
        {
           complete: (data) => {
              console.log(data)
           },
           paySuccCallback: () => {}
        }
)

sdk.pay(
        {
           'pay_type': 'midas_game_item',
           'goods_tag': 'bytest',
           'trade_no': '' + new Date().getTime(),
           'transmit_args': 'a=1&b=2',
           'indulge_auth': 1,
           'age':18,
        },
        {
           complete: (data) => {
              console.log(data)
           },
           paySuccCallback: () => {}
        }
)

sdk.pay(
      {
        'goods_tag': 'bytest',
        'trade_no': '2409104055334559122',
        'indulge_auth': 0,
        'pay_type': 'jump_miniprogram',
        'miniprogram_name': '喵星旅行'
      },
      {
        complete: (data: any) => {
          console.log('pay complete', data)
        }
      }
)

sdk.pay(
      {
        'goods_tag': 'bytest',
        'trade_no': '2409104055334559122',
        'indulge_auth': 0,
        'pay_type': 'jump_miniprogram',
        'preview_image': true,
        'miniprogram_args': {
          a: 1,
          b: 2
        },
        'miniprogram_name': '喵星旅行'
      },
      {
        complete: (data: any) => {
          console.log('pay complete', data)
        }
      }
)
    
sdk.pay(
      {
        'goods_tag': 'bytest',
        'trade_no': '2409104055334559122',
        'indulge_auth': 0,
        'envVersion': 'trial',
        'pay_type': 'jump_miniprogram',
        'miniprogram_name': '喵星旅行'
      },
      {
        complete: (data: any) => {
          console.log('pay complete', data)
        }
      }
)

sdk.pay(
      {
        'goods_tag': 'bytest',
        'trade_no': '' + new Date().getTime(),
        'indulge_auth': 0,
        'pay_type': 'wechath5'
      },
      {
        complete: (data: any) => {
          console.log('pay complete', data)
        }
      }
)
    
sdk.pay(
      {
        'pay_type': 'wxpub',
        'goods_tag': 'bytest',
        'trade_no': '' + new Date().getTime(),
        'callback_from': 0,
        'indulge_auth': 0,
        'direct_send': true,
        'title': '发送给用户的支付卡片标题',
        'desc': '发送给用户的支付卡片描述',
        'image': 'https://oss.ruixueyun.com/material/station/1722306321550.png'
      },
      {
        complete: (data: any) => {
          console.log('pay complete', data)
        }
      }
)
```

</TabItem>

<TabItem value="unity" label="Unity">

### 参数说明

| **字段**       | **类型** | **是否必填** | **备注**           |
| ------------------ | ------------ | ---------------- | --------------------------------------------- |
| pay_type       | string   | 是           | 支付的类型 ==💡 如果之前从未接过腾讯 1.0 的米大师虚拟接口（wx.reqrequestMidasPayment）需要传递'minigame_v2'==            |
| goods_tag      | string   | 是           | 计费点标识       |
| trade_no       | string   | 是           | CP 订单号             |
| indulge_auth   | number   | 是           | 是否进行防沉迷支付验证 ，0 不验证 1 验证，默认不验证如果为 1 age 必传                                                  |
| age            | number   | 否           | 用户年龄indulge_auth 为 1 时必传该字段      |
| envVersion            | string   | 否           | 跳转小程序支付 跳转小程序版本  develop 开发版 trial 体验版 release 正式版 默认为release      |
| direct_send            | boolean   | 否           | 跳转客服是否直接推送消息卡片pay_type为 wxpub 时传该字段为true跳转客服直接推送消息卡片      |
| miniprogram_name            | string   | 否           | 小游戏名称pay_type为 jump_miniprogram 时必传该字段      |
| miniprogram_args            | number   | 否           | 透传参数      |
| preview_image            | boolean   | 否           | 是否预览小程序码 长按小程序码跳转小程序支付pay_type为 jump_miniprogram 时 填写 true 可预览小程序码 长按小程序码跳转小程序支付      |
| short_url            | string   | 否           | 短域名（3.9.36废弃）preview_image为 true时必传该字段       |
| title            | string   | 否           | 跳转客服时给用户发送卡片的标题当未传递此参数时，系统会取【微信客服自动推送】中关键字为ruixue_pay_wxpub_without_card的配置，当此配置未配置时不会发送卡片，所以请保证此配置存在。      |
| desc            | string   | 否           | 跳转客服时给用户发送卡片的描述当未传递此参数时，系统会取【微信客服自动推送】中关键字为ruixue_pay_wxpub_without_card的配置，当此配置未配置时不会发送卡片，所以请保证此配置存在。      |
| image            | string   | 否           | 跳转客服时给用户发送卡片的图片当未传递此参数时，系统会取【微信客服自动推送】中关键字为ruixue_pay_wxpub_without_card的配置，当此配置未配置时不会发送卡片，所以请保证此配置存在。       |
| age            | number   | 否           | 用户年龄indulge_auth 为 1 时必传该字段      |
| notify_url     | string   | 否           | 支付成功通知 CP 发货地址                |
| func           | string   | 否           | 埋点名称 当 wxpub 时 不传func字段的时候，跳转到客服系统时右下角不弹卡片，否则右下角弹出卡片               |
| transmit_args  | string   | 否           | 客户端透传参数，key1=val1&key2=val2 的格式    |
| zoneId         | string   | 否           | 分区 ，默认'1'仅在虚拟支付有效      |
| is_debug       | number   | 否           | 是否测试订单，0 正式 1 为测试订单，默认为 0       |
| env            | number   | 否           | 是否使用沙盒环境支付，0 正式 1 沙盒，默认为 0        |
| querystr       | string   | 否           | cp 方自定义，用于打开客服透传的 url 参数， 必须以&开头【custom_description】： 为 IOS 微信公众号支付自定义订单内容详情字段例如：querystr: '&custom_description=helloSDK'💡 SDK v3.2.0 版本开始**不支持**此字段，如需传客服透传参数请使用 sessionFromExt 字段 |
| sessionFromExt | object   | 否           | sessionFrom 扩展字段，cp 方自定义，用于打开客服透传的 sessionFrom 参数，💡 请使用最新版本 sdk 【例如】：`sessionFromExt: { custom_field: 'helloSDK' }`                                                                                                                 |
| game_info     | object    | 否           | 游戏自定义数据， 上报区服和角色需要在 game_info 中传入属性，具体使用方式参考下方 game_info 字段说明  |
| bigdata_report     | object     | 否           | 商业化支付参数， 通过商业化触发的支付行为要传以下参数，否则数据统计会有丢失，具体使用方式参考下方 bigdata_report 字段说明  |
| custom_ext         | object   | 否           |自定义透传参数 |

> `CustomExt` 字段说明

| **字段**  | **类型** | **是否必填** | **备注** |
| --------- | ------  | ----------- |---------|
| bigdata_ext| object| 否             | 大数据预置事件透传参数                          |

> `bigdata_report` 字段说明

| **字段**  | **类型** | **是否必填** | **备注** |
| --------- | ------  | ----------- |---------|
| scene_identifier| string| 否             | 场景标识                           |
| scene_name     | string| 否            | 场景名称                          |
| trigger_button_identifier     | string| 否            | 触发按钮标识                         |
| trigger_button_name     | string| 否            | 触发按钮名称                         |
| window_identifier     | string| 否            | 窗口标识                         |
| window_name     | string| 否            | 窗口名称                         |
| window_sequence     | string| 否            | 窗口展示顺序                         |
| gift_package     | object     | 否            | 礼包	                         |

> `gift_package` 字段说明

| **字段**  | **类型** | **是否必填** | **备注** |
| --------- | ------  | ----------- |---------|
| identifier| string| 否             | 礼包标识                           |
| name     | string| 否            | 礼包名称                          |
| purchase_medium     | string| 否            | 购买礼包介质                         |
| price     | number| 否            | 礼包价格                         |
| billing_point     | string| 否            | 礼包计费点                         |

> `game_info` 字段说明

| **字段**  | **类型** | **是否必填** | **备注** |
| --------- | ------  | ----------- |---------|
| cp_game_character_id| string| 否             | 角色 id                           |
| cp_game_area_id     | string| 否            | 区服 id                           |

> pay_type 说明

| **字段**             | **备注**                      |
| ------------------------ |---------------------------------|
| minigame/minigame_v2 | 米大师支付【支持 Android iOS】   |
| midas_game_item      | 小游戏道具直购【支持 Android iOS】                     |
| wxpub                | ios 跳转客服公众号支付               |
| jump_miniprogram                 | 跳转小程序支付                 |
| wechath5                 | 获取h5支付链接支付                 |
| aums                 | 银联云闪付 h5 支付                 |

**调用示例**

```csharp
using System;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using RuiXue;
using RuiXue.Pay;

#if UNITY_WEBGL
using RuiXue.MiniGame.WeiXin;
#endif

public class RuiXueWeiXinMiniGameDemo : MonoBehaviour {
    public void OnPay()
    {
        var dic = new Dictionary<string, object>()
        {
            {"pay_type","minigame_v2"},
            {"goods_tag", "goods1"},
            {"trade_no", $"{DateTimeOffset.UtcNow.ToUnixTimeSeconds()}"},
            {"transmit_args", "a=1&b=2"},
            {"indulge_auth", 1},
            {"age", 18},
            {"is_debug", 1},
            {"env", 1},
        };
        
        RXPay.Pay(dic, OnResponse, OnError);
    }
    
    public void OnResponse(string data)
    {
        LogUtil.Log("RXDemo",$"Response: {data}");
    }
    
    public void OnError(string data)
    {
        LogUtil.Log("RXDemo", $"Error: {data}");
    }
}
```

</TabItem>

</Tabs>

### 响应结构

| **字段**
 | **类型**
 | **备注**
     |
| ------------ | ------------ | ---------------- |
| code
     | number
   | 0 : 表示成功
 |

## 补单

**米大师虚拟支付流程：**

通过望舒后台构建商品后调用 SDK **下单**接口，服务端验证通过后 SDK 拉起**米大师虚拟支付**进行支付，米大师虚拟支付成功后，SDK 回调**望舒服务端，**由**望舒服务端**通知**客户端服务端**进行发货。

**米大师虚拟支付补单机制：**

由于 SDK 在回调**望舒服务器**时发生了网络或各种原因，造成回调没有成功，产生了掉单，SDK 提供自动和客户端手动补单两种补偿机制。**具体如下：**

**自动补单流程**：

1. 客户端调用下单接口时 SDK 会优先判断上次支付是否掉单，如有掉单则先进行补单，**补单成功**后并移除本地缓存的补单信息，再次调用下单接口则恢复正常支付流程。**补单失败**后再次调用下单接口则继续补单。

**手动补单流程：**

1. 订单验证失败 SDK 会将订单号等补单信息回调给客户端，客户端可自行上报。
2. SDK 提供**查询是否需要补单**和**补单**功能，客户端可配合使用。建议在**支付失败**后和**登录成功**后调用。

**米大师补单流程**

![](http://guanwangyun.oss-cn-beijing.aliyuncs.com/document/payment/20231017-144034.png)

### 查询是否需要补单

**调用示例**

<Tabs groupId="platform-1">

<TabItem value="js" label="JS">

```javascript
// 同步函数
const compensateOrderInfo = sdk.checkHasCompensatePayOrder();
```

</TabItem>

<TabItem value="unity" label="Unity">

```csharp
using System;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using RuiXue;
using RuiXue.Pay;

#if UNITY_WEBGL
using RuiXue.MiniGame.WeiXin;
#endif

public class RuiXueWeiXinMiniGameDemo : MonoBehaviour {
    public void OnCheckHasCompensatePayOrder()
    {
        RXMiniGameWeiXin.CheckHasCompensatePayOrder(OnResponse, OnError);
    }
}
```

</TabItem>

</Tabs>

#### 响应结构

| **字段**
 | **类型**
 | **备注**
                                  |
| ------------ | ------------ | --------------------------------------------- |
| code
     | number
   | 0 : 表示成功，需要补单 
-1：不需要补单
 |
| msg
      | string
   | 'had': 需要补单 ，'null'：不需要补单
      |
| check
    | object
   | 补单信息，需要补单时有此字段
              |
| data
     | object
   | null，不需要补单时有此字段
                |

> `check` 补单信息

| **字段**
   | **类型**
 | **备注**
      |
| -------------- | ------------ | ----------------- |
| notify_url
 | string
   | 回调的地址
    |
| wx_openid
  | string
   | 微信的 openid
 |
| order_no
   | string
   | 订单号
        |
| amount
     | number
   | 计费点数
      |
| env
        | number
   | 环境
          |
| zone_id
    | string
   | 分区
          |
| pf
         | string
   | 平台
          |

### 补单

:::tip
参数从查询是否需要补单接口获取
:::

<Tabs groupId="platform-2">

<TabItem value="js" label="JS">

#### 参数说明

| **字段** | **类型**      | **是否必填** | **备注**     |
| ------------ | ----------------- | ---------------- | ---------------- |
| params   | ICheckOrder   | 是           | 参数         |
| callback | IMethodParams | 是           | 回调函数集合 |

> ICheckOrder 说明

| **字段**   | **类型** | **是否必填** | **备注**      |
| -------------- | ------------ | ---------------- | ----------------- |
| notify_url | string   | 是           | 回调的地址    |
| wx_openid  | string   | 是           | 微信的 openid |
| order_no   | string   | 是           | 订单号        |
| amount     | number   | 是           | 计费点数      |
| env        | number   | 是           | 环境          |
| zone_id    | string   | 是           | 分区          |
| pf         | string   | 是           | 平台          |

> IMethodParams 说明

| **字段** | **备注**                                                  |
| ------------ | ------------------------------------------------------------- |
| complete | (data) => voidcomplete：为函数且必传，data 为结果数据 |

**调用示例**
```javascript
sdk.compensatePayOrder(
  {
    notify_url, //回调的地址
    wx_openid, //微信的openid
    order_no, //订单号
    amount, //计费点数
    env, //环境
    zone_id, //分区
    pf, //平台
  },
  {
    complete: (data) => {
      console.log(data); //code为0即为补单成功其他均为失败
    },
  }
);
```

</TabItem>

<TabItem value="unity" label="Unity">

#### 参数说明

| **字段**   | **类型** | **是否必填** | **备注**      |
| -------------- | ------------ | ---------------- | ----------------- |
| notify_url | string   | 是           | 回调的地址    |
| wx_openid  | string   | 是           | 微信的 openid |
| order_no   | string   | 是           | 订单号        |
| amount     | number   | 是           | 计费点数      |
| env        | number   | 是           | 环境          |
| zone_id    | string   | 是           | 分区          |
| pf         | string   | 是           | 平台          |

**调用示例**

```csharp
using System;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using RuiXue;
using RuiXue.Pay;

#if UNITY_WEBGL
using RuiXue.MiniGame.WeiXin;
#endif

public class RuiXueWeiXinMiniGameDemo : MonoBehaviour {
    public void OnCompensatePayOrder()
    {
        var dic = new Dictionary<string, object>()
        {
            {"notify_url","http://cn-api-test.ruixuecloud.com/"},
            {"goods_tag", "goods_tag_test"},
            {"wx_openid", "aljgklalkg"},
            {"amount", 1},
            {"order_no", $"{DateTimeOffset.UtcNow.ToUnixTimeSeconds()}"},
            {"transmit_args", "a=1&b=2"},
            {"env", 1},
            {"zone_id", "1"},
            {"pf", "android"}
        };
        
        RXMiniGameWeiXin.CompensatePayOrder(dic, OnResponse, OnError);
    }
}
```

</TabItem>

</Tabs>

## 支付错误码

| 错误码
 | 含义
                                                                             |
| ---------- | ------------------------------------------------------------------------------------ |
| 0
      | 成功,无错误
                                                                      |
| 142601
 | 服务内部错误
                                                                     |
| 142602
 | 服务内部错误 2
                                                                   |
| 152403
 | 服务内部错误 3
                                                                   |
| 152404
 | 服务内部错误 4
                                                                   |
| 312605
 | 望舒内部下单失败
                                                                 |
| 152406
 | 客户端支付回调请求验单失败
                                                       |
| 152407
 | 客户端支付回调望舒失败（客户端需重新发起重试）
                                   |
| 152408
 | 客户端望舒回调金额错误
                                                           |
| 152409
 | 获取订单失败（订单不存在）
                                                       |
| 152410
 | 望舒请求三方 HTTP 请求失败
                                                       |
| 152411
 | 望舒解析三方请求失败
                                                             |
| 152412
 | 防沉迷提示语不存在 望舒后台未配置
                                                |
| 152413
 | 微信小游戏 sessionkey 过期  SDK 需重新同步用户信息刷新 sessionkey 在重试一次下单
 |
| 302402
 | SDK 请求参数错误
                                                                 |
| 302403
 | SDK 请求支付类型字段 pay_type 未传
                                               |
| 302404
 | SDK 请求非法商品(检查 goods_tag 请求参数和望舒后台计费点配置)
                    |
| 302405
 | SDK 请求用户 openid 未传
                                                         |
| 302406
 | SDK 请求支付类型未配置
                                                           |
| 302407
 | SDK 支付回调 order_no 订单号未传
                                                 |
| 302408
 | SDK 支付重复回调订单(订单已处理过)
                                               |
| 302409
 | SDK 支付回调支付凭证错误
                                                         |
| 302410
 | SDK 支付回调用户三方余额不足(扣款失败)
                                           |
| 302411
 | SDK 支付回调支付凭证解析结果状态为未支付
                                         |
| 302412
 | 用户触发防沉迷充值机制 不可充值
                                                  |
