---
sidebar_position: 2
title: "JavaScript 接入"
---

## 上报大数据

:::tip
需要在 SDK 初始化后调用！！！
:::

* ( ) JS::jsinit1
* ( ) Unity::jsinit2

## 设置公共属性

设置后由 SDK 填入自定义属性中进行上报，设置后 SDK 进行缓存，多次设置则会覆盖。

* ( ) JS::jsinit3
* ( ) Unity::jsinit4

### 响应结构

成功 code 为 0

## 修改公共属性

修改的属性会将原有属性覆盖，未设置的属性则会补入缓存中。

* ( ) JS::jsinit5
* ( ) Unity::jsinit6

### 响应结构

成功 code 为 0

## 删除公共属性

删除缓存的某个公共属性。
* ( ) JS::jsinit7
* ( ) Unity::jsinit8

### 响应结构

成功 code 为 0