---
sidebar_position: 2
title: "JavaScript 接入"
---

## 定位

### 授权定位

:::tip
获取当前的地理位置
微信基础库 2.25.0 开始支持，请注意当前设置的微信基础库版本。
:::

* ( ) JS::social1
* ( ) Unity::social2

#### 响应结构
| **字段**
 | **类型**
 | **备注**
                                                                                                |
| ------------ | ------------ | ----------------------------------------------------------------------------------------------------------- |
| code
     | number
   | 0: 表示成功
6020: 表示定位失败
6021: 表示定位被拒绝
                                               |
| data
     | object
   | 经纬度信息
 |
| data.latitude
     | number
   | 纬度，范围为 -90~90，负数表示南纬
 |
| data.longitude
     | number
   | 经度，范围为 -180~180，负数表示西经
 |

### 获得附近人

* ( ) JS::social3
* ( ) Unity::social4

#### 响应结构

**HTTP 状态码：**`200 OK`

| **字段** | **类型**                                  | **是否必填** | **说明** | **示例** |
| --- |-----------------------------------------| --- | --- | --- |
| code | Number                                  | 是   | 响应状态码，0为成功，非0表示出错 | 0   |
| msg | String                                  | 否   | 响应信息，仅在status为非0时不为空 |     |
| data | [DataNode](/docs/v3.5/社交/功能介绍) | 否   | 结果数据集 |     |

##### DataNode

| **字段** | **类型** | **是否必填** | **说明** | **示例** |
| --- | --- | --- | --- | --- |
| openid | String | 是   | 用户望舒 OpenID |     |
| nickname | String | 否   | 用户昵称 |     |
| birthday | String | 否   | 用户生日日期 |     |
| remarks | String | 否   | 当前用户对其的备注 |     |
| avatar | String | 否   | 用户头像 |     |
| custom | String | 否   | CP 自定义信息 |     |
| dist | Float64 | 是   | 距离当前用户的距离（米） |     |
| sex | Number | 否   | 用户性别，1:男；0:女 |     |
| cp_userid | Number | 否   | CP侧 用户id |     |

### 开启定位上报

* ( ) JS::social5
* ( ) Unity::social6

#### 响应结构

| **字段**
 | **类型**
 | **备注**
                                                                                                |
| ------------ | ------------ | ----------------------------------------------------------------------------------------------------------- |
| code
     | number
   | 0: 表示成功
6020: 表示定位失败
6021: 表示定位被拒绝
                                               |
| data
     | object
   | 请参考 [上报/更新经纬度坐标 API](/docs/v3.5/社交/功能介绍) 响应结构。
 |

### 关闭定位上报

**调用示例**
* ( ) JS::social7
* ( ) Unity::social8

### 删除位置信息

* ( ) JS::social9
* ( ) Unity::social10

#### 响应结构

**HTTP 状态码：**`200 OK`

| **字段** | **类型** | **是否必填** | **说明** | **示例** |
| --- | --- | --- | --- | --- |
| code | Number | 是   | 响应状态码，0为成功，非0表示出错 | 0   |
| msg | String | 否   | 响应信息，仅在status为非0时不为空 |     |

## 设置自定义关系

### 添加自定义关系

* ( ) JS::social11
* ( ) Unity::social12

#### 响应结构

**HTTP 状态码：**`200 OK`

| **字段** | **类型** | **是否必填** | **说明** | **示例** |
| --- | --- | --- | --- | --- |
| code | Number | 是   | 响应状态码，0为成功，非0表示出错 | 0   |
| msg | String | 否   | 响应信息，仅在status为非0时不为空 |     |

### 删除自定义关系

* ( ) JS::social13
* ( ) Unity::social14

#### 响应结构

**HTTP 状态码：**`200 OK`

| **字段** | **类型** | **是否必填** | **说明** | **示例** |
| --- | --- | --- | --- | --- |
| code | Number | 是   | 响应状态码，0为成功，非0表示出错 | 0   |
| msg | String | 否   | 响应信息，仅在status为非0时不为空 |     |

### 更新自定关系备注

* ( ) JS::social15
* ( ) Unity::social16

#### 响应结构

**HTTP 状态码：**`200 OK`

| **字段** | **类型** | **是否必填** | **说明** | **示例** |
| --- | --- | --- | --- | --- |
| code | Number | 是   | 响应状态码，0为成功，非0表示出错 | 0   |
| msg | String | 否   | 响应信息，仅在status为非0时不为空 |     |

### 判断两用户是否存在某自定关系

* ( ) JS::social17
* ( ) Unity::social18

#### 响应结构

**HTTP 状态码：**`200 OK`

| **字段** | **类型** | **是否必填** | **说明** | **示例** |
| --- | --- | --- | --- | --- |
| code | Number | 是   | 响应状态码，0为成功，非0表示出错 | 0   |
| msg | String | 否   | 响应信息，仅在status为非0时不为空 |     |
| data | Bool | 是   | 存在关系时为 true；否则为 false |     |

### 获取自定关系列表

* ( ) JS::social19
* ( ) Unity::social20

#### 响应结构

**HTTP 状态码：**`200 OK`

| **字段** | **类型**                         | **是否必填** | **说明** | **示例** |
| --- |--------------------------------| --- | --- | --- |
| code | Number                         | 是   | 响应状态码，0为成功，非0表示出错 | 0   |
| msg | String                         | 否   | 响应信息，仅在status为非0时不为空 |     |
| data | [][Node](/docs/v3.5/社交/功能介绍) | 否   | 结果数据集 |     |

##### Node

| **字段** | **类型** | **是否必填** | **说明** | **示例** |
| --- | --- | --- | --- | --- |
| openid | String | 是   | 用户望舒 OpenID |     |
| nickname | String | 否   | 用户昵称 |     |
| birthday | String | 否   | 用户生日日期 |     |
| remarks | String | 否   | 当前用户对其的备注 |     |
| avatar | String | 否   | 用户头像 |     |
| custom | String | 否   | CP 自定义信息 |     |
| time | Number | 否   | 关系形成时间戳（单位：秒） |     |
| sex | Number | 否   | 用户性别，1:男；0:女 |     |
| cp_userid | Number | 否   | CP侧 用户id |     |

## 设置好友关系

### 添加好友

* ( ) JS::social21
* ( ) Unity::social22

#### 响应结构

**HTTP 状态码：**`200 OK`

| **字段** | **类型** | **是否必填** | **说明** | **示例** |
| --- | --- | --- | --- | --- |
| code | Number | 是   | 响应状态码，0为成功，非0表示出错 | 0   |
| msg | String | 否   | 响应信息，仅在status为非0时不为空 |     |

### 删除好友

* ( ) JS::social23
* ( ) Unity::social24

#### 响应结构

**HTTP 状态码：**`200 OK`

| **字段** | **类型** | **是否必填** | **说明** | **示例** |
| --- | --- | --- | --- | --- |
| code | Number | 是   | 响应状态码，0为成功，非0表示出错 | 0   |
| msg | String | 否   | 响应信息，仅在status为非0时不为空 |     |

### 更新好友备注

* ( ) JS::social25
* ( ) Unity::social26

#### 响应结构

**HTTP 状态码：**`200 OK`

| **字段** | **类型** | **是否必填** | **说明** | **示例** |
| --- | --- | --- | --- | --- |
| code | Number | 是   | 响应状态码，0为成功，非0表示出错 | 0   |
| msg | String | 否   | 响应信息，仅在status为非0时不为空 |     |

### 判断两用户是否为好友

* ( ) JS::social27
* ( ) Unity::social28

#### 响应结构

**HTTP 状态码：**`200 OK`

| **字段** | **类型** | **是否必填** | **说明** | **示例** |
| --- | --- | --- | --- | --- |
| code | Number | 是   | 响应状态码，0为成功，非0表示出错 | 0   |
| msg | String | 否   | 响应信息，仅在status为非0时不为空 |     |
| data | Bool | 是   | 存在关系时为 true；否则为 false |     |

### 获取好友关系列表

* ( ) JS::social29
* ( ) Unity::social30

#### 响应结构

**HTTP 状态码：**`200 OK`

| **字段** | **类型**                         | **是否必填** | **说明** | **示例** |
| --- |--------------------------------| --- | --- | --- |
| code | Number                         | 是   | 响应状态码，0为成功，非0表示出错 | 0   |
| msg | String                         | 否   | 响应信息，仅在status为非0时不为空 |     |
| data | [][Node](/docs/v3.5/社交/功能介绍) | 否   | 结果数据集 |     |