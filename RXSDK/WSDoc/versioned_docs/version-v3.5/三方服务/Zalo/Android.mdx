---
title: "Android"
---

重要：请先查看渠道接入说明！[快速入口](/docs/v3.5/入门指南/快速开始/Android初始化/快速接入) 

## SDK 集成

```xml
//Zalo 引用此库
implementation 'com.ruixue:rxsdk_zalo:${version}'
```

## 参数说明

| 参数 | 类型 | 说明 |
| ---- | ---- | ---- |
| zaloAppId | Long | string.xml 中配置 ZALO_APP_ID 值 |

## 配置说明
* 在您的项目根目录中，打开 `your_app | Gradle Scripts | build.gradle (Project) `并添加以下存储库：
```groovy
allprojects {
    repositories {
        maven {
            url "https://gitlab.com/api/v4/projects/50747855/packages/maven"
            name "GitLabZaloSDK"
        }
    }
}
```

- 在 `src/mian/res/values/strings` 文件下

```xml
 1290303975374472026
```

- 在 AndroidManifest.xml 配置

```xml

    

    

            
                

                
                
                <!-- zalo- 加 app id -->
                
            
                
            
```

`注`：由于 meta-data 不支持 long 数据类型，如果配置的值为数字类型，则默认为 int 值，导致无法参数错误，所以参数需要配置在 `strings.xml` 文件内。

## 三方版本信息

使用 `4.24.1101` 版本

## 依赖信息

使用 `aar` 接入方式的的请添加下面依赖配置，否则无需添加

```
//zalo sdk
api "me.zalo:sdk-core:4.24.1101"
api "me.zalo:sdk-auth:4.24.1101"
api "me.zalo:sdk-openapi:4.24.1101"
```

## 登录

[点此跳转到 UI 登录方式](/docs/v3.5/防沉迷/功能介绍)
[点此跳转到 API 登录方式](/docs/v3.5/登录（通行证）/Android 登录API/快速接入)
## 分享

[点此跳转到分享 API](/docs/v3.5/三方服务/Android 渠道接入说明)

## 关于 Android Hash Key 的生成

将下面方法放到自己的项目中，在 Application 初始化的时候，调用并打印出来即可，给后台配置后就可以删除掉该方法了。

```java
 /**
  * 此方法用来根据包名和证书生成 key，给配置后台配置即可
  *
  * @param ctx
  * @return
  * @throws Exception
  */
private static String getApplicationHashKey(Context ctx) throws Exception {
    PackageInfo info = ctx.getPackageManager().getPackageInfo(ctx.getPackageName(), PackageManager.GET_SIGNATURES);
    for (Signature signature : info.signatures) {
        MessageDigest md = MessageDigest.getInstance("SHA");
        md.update(signature.toByteArray());
        String sig = Base64.encodeToString(md.digest(), Base64.DEFAULT).trim();
        if (sig.trim().length() > 0) {
            return sig;
        }
    }
    return null;
}
```