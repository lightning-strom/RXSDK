---
title: "推送"
---

## SDK集成
```json
//海外渠道引用此库
  implementation 'com.ruixue:rxsdk_firebase:${version}'
```

## 设置 Android 客户端

[Firebase 官方文档](https://firebase.google.com/docs/cloud-messaging/android/client?hl=zh-cn)

**防止自动初始化**
在生成 FCM 注册令牌后，库会将标识符和配置数据上传到 Firebase。如果您希望阻止自动生成令牌，请将以下元数据值添加到 AndroidManifest.xml，以停用 Analytics 数据收集和 FCM 自动初始化功能（您必须同时停用这两项功能）：

```xml
    
    
```

如需重新启用 FCM 自动初始化功能，请执行运行时调用：

**接口原型**

```java
    /**
     * 重新启用 FCM 自动初始化功能
     * @param enable 是否开启
     */
    public void setAutoInitEnabled(boolean enable);
```

**调用示例**

```java
    FirebaseSdkWrapper.getInstance().getFirebaseMessaging().setAutoInitEnabled(true);
```

## 向后台应用发送测试消息

[向后台应用发送测试消息](https://firebase.google.com/docs/cloud-messaging/android/first-message?hl=zh-cn)

项目创建以及全局 gradle 配置参照 Firebase 官方文档
[发送测试消息](https://firebase.google.com/docs/cloud-messaging/android/first-message?hl=zh-cn)

::: tip
官方 firebase-crashlytics SDK 无需引入，只需按以下方式接入望舒SDK即可。
:::

#### 在 Android 13 及更高版本上请求运行时通知权限

Android 13 中引入了用于显示通知的新运行时权限。该项引入会影响在 Android 13 或更高版本上使用 FCM 通知的所有应用。

默认情况下，FCM SDK（23.0.6 或更高版本）中包含清单中定义的 POST_NOTIFICATIONS 权限。不过，您的应用还需要通过常量 android.permission.POST_NOTIFICATIONS 请求此权限的运行时版本。在用户授予此权限之前，您的应用将无法显示通知。

```java
// Declare the launcher at the top of your Activity/Fragment:
private final ActivityResultLauncher requestPermissionLauncher =
        registerForActivityResult(new ActivityResultContracts.RequestPermission(), isGranted -> {
            if (isGranted) {
                // FCM SDK (and your app) can post notifications.
            } else {
                // TODO: Inform user that that your app will not show notifications.
            }
        });

private void askNotificationPermission() {
    // This is only necessary for API level >= 33 (TIRAMISU)
    if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU) {
        if (ContextCompat.checkSelfPermission(this, Manifest.permission.POST_NOTIFICATIONS) ==
                PackageManager.PERMISSION_GRANTED) {
            // FCM SDK (and your app) can post notifications.
        } else if (shouldShowRequestPermissionRationale(Manifest.permission.POST_NOTIFICATIONS)) {
            // TODO: display an educational UI explaining to the user the features that will be enabled
            //       by them granting the POST_NOTIFICATION permission. This UI should provide the user
            //       "OK" and "No thanks" buttons. If the user selects "OK," directly request the permission.
            //       If the user selects "No thanks," allow the user to continue without notifications.
        } else {
            // Directly ask for the permission
            requestPermissionLauncher.launch(Manifest.permission.POST_NOTIFICATIONS);
        }
    }
}
```

通常，您应向用户显示一个界面，说明如果用户授予应用发布通知的权限，会启用哪些功能。此界面应向用户提供同意或拒绝的选项，例如确定和不用了按钮。如果用户选择确定，则直接请求该权限。如果用户选择不用了，则允许用户在不接收通知的情况下继续操作。

详细说明见 [官方文档](https://firebase.google.com/docs/cloud-messaging/android/client?hl=zh&authuser=0)

```json
//海外渠道引用此库
  implementation 'com.ruixue:rxsdk_firebase:${version}'
```

## 推送接口说明

`RxPushManager` 是望舒推送在 Android 平台的接口类。该类提供了一系列静态方法（static）接口，无需实例化直接调用。

:::tip
使用望舒 **推送自定义透传参数（deeplink）** 功能获取参数参考下方 `parseDeeplink` 示例函数。
:::

**接口原型**

```java
/**
 * @param context Android平台上app的上下文
 * @return 是否初始化成功，true 初始化成功当前平台支持推送， false 不支持
 */
public static boolean init(@NonNull Context context) ;

/**
 * 处理打开app回调，用于统计通知点击
 * 在带有  标记的，
 * 启动 Activity 中 onCreate 和  onNewIntent 函数中调用此函数,否则可能影响统计。
 * @param intent 传入getIntent()
 */
public static void openAppCallback(Intent intent) ;
```

```java
/**
 * 注册望舒推送服务（建议望舒登录成功后调用一次，不切换账号不用重复调用）
   由于registerToken和望舒账号关联绑定，所以请不要在登录前调用
 */
public static void registerToken() ;

/**
 * 反注册望舒推送服务（建议在登出账号时调用）
 */
public static void unRegisterToken(;
/**
 * @return 服务器生成的推送 token/regId
 */
public static String getDeviceToken();

/**
 * @return 当前设备是否支持推送 init（）后调用
 */
public static boolean isSupport();

/**
 * @return 当前推送的厂商平台 BRAND
 */
public static String getBrandName();

/**
 * 开发者可以为指定用户设置别名，然后给这个别名推送消息，效果等同于给RegId推送消息。
 *
 * @param alias 要绑定的别名
 */
public static void bindAlias(@NonNull String alias) ;

/**
 * 开发者可以取消指定用户的某个别名，服务器就不会给这个别名推送消息了。
 *
 * @param alias 要取消的别名 ，空取消所有别名
 */
public static void unBindAlias(String alias);
```

**调用示例**

```java
 /**
     *  使用望舒推送自定义透传参数的解析示例,客户端解析通过通知点击携带的服务端参数。
     * @param intent 在 LAUNCHER Activity 的 onCreate 或 onNewIntent 中获取 deeplink ，并调用 openAppCallback 传 intent 统计点击
     */
    private void parseDeeplink(Intent intent) {
        try {
            String ob = intent.getStringExtra("payload");
            JSONObject jsonObject = new JSONObject(ob);
            Log.i("deeplink",jsonObject.toString());
            String deeplink=jsonObject.optString("deeplink");
            //todo 解析 deeplink 值,执行相应业务逻辑
            AlertTipView.create(MainPushActivity.this, "deeplink",deeplink , null).setContentGravity(Gravity.START).show();
        } catch (JSONException e) {
             e.printStackTrace();
        }
    }

    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);

        RxPushManager.init(this);
        RxPushManager.openAppCallback(getIntent());
        parseDeeplink(getIntent);
        ......
    }

    @Override
    public void onNewIntent(Intent intent) {
        super.onNewIntent(intent);
        setIntent(intent);
        RxPushManager.openAppCallback(intent);
        parseDeeplink(intent);

        ......
    }
    
    //望舒登录成功后注册 token
     public void onRuiXueLoginScuccess() {
        RxPushManager.registerToken();
        ......
    }
```

**推送设备注册结果自查方法**

在SDK `RuiXueSdk.setDebugEnabled(true);` 开启debug时，在logcat控制台日志搜索registerToken，会有类似如下日志输出。

![](https://guanwangyun.oss-cn-beijing.aliyuncs.com/document/push/XyKabFO1soh2AzxGulwc21cdn7d.png)

设备注册成功时的logcat输出示例

```
registerToken token:IQAAAACy0omCAACJk15CXKuzBZu6oE4LeUsl3hdcCepcaD8UeJVIWen4NBZhPgJOjL6Y_zvcfDgKThaixLFiaLbzS5_0UMF7y8fZFvuXb3Euzbz4mw
```

`或`
 手动获取 registerToken 检查是否注册成功

```
// token 不为空时，说明已注册成功。
String token =RxPushManager.getDeviceToken();

```

---

::: tip
如果通过望舒集成 firebase 推送，调用上面的望舒 API 即可完成
下面的接口方法是为接入方想要调用 firebase 接口自行接入推送而提供的，可参考下面的接口
:::

## 获取注册令牌

如需将消息发送到特定的设备，您需要知道该设备的注册令牌。您需要在 Notifications 控制台的相应字段中输入该令牌才能完成本教程，因此请确保在获得令牌后即复制该令牌或将其妥善存储。

初次启动您的应用时，FCM SDK 会为客户端应用实例生成一个注册令牌。如果您希望指定单一目标设备或者创建设备组，需要扩展 RxFirebaseMessagingService 并重写 onNewToken 来获取此令牌。

本部分介绍如何检索令牌以及如何监控令牌的变更。因为令牌可能会在初始启动后轮替，所以我们强烈建议您检索最近更新的注册令牌。

注册令牌可能会在发生下列情况时更改：

- 应用在新设备上恢复
- 用户卸载/重新安装应用
- 用户清除应用数据。

::: tip
对于仍在使用已弃用的实例 ID API 进行令牌管理的应用，您应更新所有令牌逻辑，使用下述 FCM API。
:::

**检索当前注册令牌**
**接口原型**

```java
    /**
     * 检索当前注册令牌
     * @param onCompleteListener 回调
     */
    public void getToken(OnCompleteListener onCompleteListener);
```

**调用示例**

```java
    FirebaseSdkWrapper.getInstance().getFirebaseMessaging().getToken(new OnCompleteListener() {
        @Override
        public void onComplete(@NonNull Task task) {
            Log.d("firebase-message", "getToken:" + task.toString());
        }
    });
```

**监控令牌的生成**

每当生成新令牌时，都会触发 onNewToken 回调函数。

```java
    @Override
    public void onNewToken(@NonNull String token) {
        super.onNewToken(token);
    }
```

## 向多台设备发送消息

[向多台设备发送消息](https://firebase.google.com/docs/cloud-messaging/android/send-multiple?hl=zh-cn)

**为客户端应用订阅主题**

**接口原型**

```java
    /**
     * 为客户端应用订阅主题
     * @param topic 主题
     * @param onCompleteListener 监听回调
     */
    public void subscribeToTopic(String topic, OnCompleteListener onCompleteListener);
```

**调用示例**

```java
    FirebaseSdkWrapper.getInstance().getFirebaseMessaging().subscribeToTopic("weather", new OnCompleteListener() {
        @Override
        public void onComplete(@NonNull Task task) {
            Log.d("firebase-message", "subscrib: " + task.toString());
        }
    });
```

## 在通知载荷中发送图片

[在通知载荷中发送图片](https://firebase.google.com/docs/cloud-messaging/android/send-image?hl=zh-cn)

此外，建议您设置默认值以便自定义通知的外观。您可以指定自定义默认图标和颜色，当通知载荷中未设置相应的值时，系统会使用这些默认值。

将以下代码行添加到 application 标记内，以设置自定义默认图标和颜色：

```xml
<!-- Set custom default icon. This is used when no icon is set for incoming notification messages.
     See README(https://goo.gl/l4GJaQ) for more. -->

<!-- Set color used with incoming notification messages. This is used when no color is set for the incoming
     notification message. See README(https://goo.gl/6BKBk7) for more. -->

```

## 在直接启动模式下接收 FCM 消息

具体配置，参照官网。
::: tip
firebase-messaging-directboot 无需自行引入，望舒SDK已包含
:::

## 设置和管理消息优先级

[设置和管理消息优先级](https://firebase.google.com/docs/cloud-messaging/android/message-priority?hl=zh-cn)

## 主题消息传递

[主题消息传递](https://firebase.google.com/docs/cloud-messaging/android/topic-messaging?hl=zh-cn)

## 向设备组发送消息

[向设备组发送消息](https://firebase.google.com/docs/cloud-messaging/android/device-group?hl=zh-cn)

## 发送上行消息

[发送上行消息](https://firebase.google.com/docs/cloud-messaging/android/upstream?hl=zh-cn)

**从 Android 客户端应用发送上行消息**
Android 应用能使用 send 发送上行消息。

**接口原型**

```java
    /**
     * 向服务端发送上行消息
     * @param message 消息
     */
    public void send(RemoteMessage message);
```

**调用示例**

```java
    RemoteMessage remoteMessage =
            new RemoteMessage.Builder(SENDER_ID + "@fcm.googleapis.com")
            .setMessageId(Integer.toString(1111))
            .addData("my_message", "Hello World")
            .addData("my_action","SAY_HELLO")
            .build();
    FirebaseSdkWrapper.getInstance().getFirebaseMessaging().send(remoteMessage);
```

## 使用 Firebase 控制台发送消息

[使用 Firebase 控制台发送消息](https://firebase.google.com/docs/cloud-messaging/android/send-with-console?hl=zh-cn)