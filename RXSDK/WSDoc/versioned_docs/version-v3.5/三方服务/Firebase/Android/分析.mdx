---
title: "分析"
---

## 创建项目

项目创建以及全局 gradle 配置参照 Firebase 官方文档
[准备工作](https://firebase.google.com/docs/analytics/get-started?hl=zh-cn&platform=android)
[将 Firebase 添加到您的 Android 项目](https://firebase.google.com/docs/android/setup?hl=zh-cn#groovy)

## SDK集成

::: tip
官方 firebase-analytics SDK 无需引入，只需按以下方式接入望舒SDK即可。
:::

```json
//海外渠道引用此库
  implementation 'com.ruixue:rxsdk_firebase:${version}'
```

## 初始化SDK

**接口原型**

```java
        /**
     * 初始化fireBase
     * @param context Context上下文
     */
    public void initFirebaseAnalytics(Context context);
```

**调用示例**

```java
FirebaseSdkWrapper.getInstance().initFirebaseAnalytics(this);
```

## 记录事件

初始化 FirebaseAnalytics 后，您就可以使用 logEvent() 方法来记录事件。

为帮助您着手使用，Analytics SDK 定义了许多常见于各类型应用（零售、电子商务、旅行以及游戏应用等）的推荐事件。如需详细了解[这些事件](https://support.google.com/analytics/answer/9267735?hl=zh-cn)及其适用情形，请参阅[推荐事件](https://support.google.com/analytics/answer/9267735?hl=zh-cn)。

::: tip
注意：为了让您的报告尽可能详尽，请记录对您的应用有意义的推荐事件及其预设参数。这也能确保您在最新的 Google Analytics（分析）功能推出时马上就能受益。
:::

您可在以下位置找到推荐的事件类型的实现详情：

- 推荐事件：请参阅 [com.google.firebase.analytics.FirebaseAnalytics.Event](https://firebase.google.com/docs/reference/android/com/google/firebase/analytics/FirebaseAnalytics.Event?hl=zh-cn) 类参考文档。
- 预设参数：请参阅 [com.google.firebase.analytics.FirebaseAnalytics.Param](https://firebase.google.com/docs/reference/android/com/google/firebase/analytics/FirebaseAnalytics.Param?hl=zh-cn) 参考文档。
下面的示例演示了如何记录 SELECT_CONTENT 事件：

::: tip
注意：引入望舒 Firebase SDK后， FirebaseAnalytics.Param 替换为 RxFirebaseAnalytics.Param， FirebaseAnalytics.Event 替换为 RxFirebaseAnalytics.Event
:::

**接口原型**

```java
    /**
     * 记录事件
     * @param name 事件类型
     * @param params 事件数据
     */
    public void logEvent(String name, @Nullable Bundle params);
```

**调用示例**

```java
    Bundle bundle = new Bundle();
    bundle.putString(RxFirebaseAnalytics.Param.ITEM_ID, id);
    bundle.putString(RxFirebaseAnalytics.Param.ITEM_NAME, name);
    bundle.putString(RxFirebaseAnalytics.Param.CONTENT_TYPE, "image");
    FirebaseSdkWrapper.getInstance().getFirebaseAnalytics().logEvent(RxFirebaseAnalytics.Event.SELECT_CONTENT, bundle);
```

除了预设参数外，您还可以将以下参数添加到事件中：

- 自定义参数：可使用自定义参数作为 Analytics 报告中的维度或指标。 您可以对非数值事件参数数据使用自定义维度，而对更适合以数值形式表示的参数数据使用自定义指标。使用 SDK 记录自定义参数后，请注册维度或指标，以确保这些自定义参数能够出现在 Analytics 报告中。可依次点击以下各项完成此操作：“分析”>“事件”>“管理自定义设置”>“创建自定义维度”。
自定义参数可用于每份报告都可能使用的受众群体定义中。如果您的应用已关联到 BigQuery 项目，自定义参数也将包含在导出至 BigQuery 的数据中。如需查看示例查询及更多内容，请访问 Google Analytics（分析）4 BigQuery Export。
- VALUE 参数：VALUE 是一个通用参数，可用于累积与事件相关的关键指标，例如收入、距离、时间和分数。

如果推荐的事件类型未涵盖您的应用的特定需求，您可以按以下示例所示的方式记录自己的自定义事件：

**调用示例**

```java
    Bundle bundle = new Bundle();
    Bundle params = new Bundle();
    params.putString("image_name", name);
    params.putString("full_text", text);
    FirebaseSdkWrapper.getInstance().getFirebaseAnalytics().logEvent("share_image", bundle);
```

## 设置默认事件参数

您可以使用 setDefaultEventParameters 跨事件记录参数。默认参数会与记录的所有将来的事件相关联。
与自定义参数一样，请注册默认事件参数，以确保这些参数能显示在 Analytics 报告中。

**接口原型**

```java
    /**
     * 设置默认事件参数
     * @param params 事件bundle
     */
    public void setDefaultEventParameters(@Nullable Bundle params);
```

**调用示例**

```java
    Bundle parameters = new Bundle();
    parameters.putString("level_name", "Caverns01");
    parameters.putInt("level_difficulty", 4);
    FirebaseSdkWrapper.getInstance().getFirebaseAnalytics().setDefaultEventParameters(parameters);
```

如果您在 logEvent() 方法中指定了某个参数，系统将使用该值而非默认值。
如需清除默认参数，请调用 setDefaultEventParameters 方法，并将参数设置为 null。

## 设置用户属性

您可以设置 Analytics 用户属性来描述应用的用户，还可以创建自定义设置，然后在报告中用这些设置进行对比或用作受众群体评估条件，来使用用户属性。
如需设置用户属性，请按以下步骤操作：

- 在 Firebase 控制台的 Analytics 的 [Custom Definitions（自定义设置）页面](https://console.firebase.google.com/project/_/analytics/userproperty?hl=zh-cn&_gl=1*w4pl1n*_ga*MTI3Njk0NTI3Ny4xNjkxMDUxMjYw*_ga_CW55HF8NVT*MTY5Mjc2MTUyNC4yMy4wLjE2OTI3NjE2NDMuMC4wLjA.)中，为用户属性创建自定义设置。如需了解详情，请参阅[自定义维度和指标](https://support.google.com/firebase/answer/6317519?hl=zh-cn)。
- 使用 setUserProperty() 方法在您的应用中设置用户属性。

下面的示例展示了如何添加一个假设的“最喜欢的食物”属性，将 mFavoriteFood 中的值分配给活跃用户：

**接口原型**

```java
    /**
     * 设置用户属性
     * @param name 属性名
     * @param value 属性值
     */
    public void setUserProperty(String name, String value);
```

**调用示例**

```java
    FirebaseSdkWrapper.getInstance().getFirebaseAnalytics().setUserProperty("favorite_food", "food");
```

::: tip
注意：注册属性后，系统可能需要几个小时的时间才能将通过该属性收集到的数据纳入报告中。当新数据可用时，该用户属性可用作报告过滤条件或受众群体定义。
:::

您可以在 Firebase 控制台的 Analytics 的 [Custom Definitions（自定义设置）页面](https://console.firebase.google.com/project/_/analytics/userproperty?hl=zh-cn&_gl=1*katj34*_ga*MTI3Njk0NTI3Ny4xNjkxMDUxMjYw*_ga_CW55HF8NVT*MTY5Mjc2NzgxMy4yNC4wLjE2OTI3Njc4MTMuMC4wLjA.)中访问这些数据。该页面会显示您已为应用定义的用户属性列表。您可以在 Google Analytics（分析）提供的多份报告比较中使用这些属性。如需详细了解 [Analytics 报告信息中心](https://support.google.com/firebase/answer/6317517?hl=zh-cn)，请访问 Firebase 帮助中心。

## 设置默认事件参数

参考官网 [调试事件](https://firebase.google.com/docs/analytics/debugview?hl=zh-cn#android)

## 跟踪屏幕

参考官网 [跟踪屏幕](https://firebase.google.com/docs/analytics/screenviews?hl=zh-cn#java)

## 设置用户 ID

**接口原型**

```java
    /**
     * 设置用户ID
     * @param id 用户ID
     */
    public void setAnalyticsUserId(String id);
```

**调用示例**

```java
    FirebaseSdkWrapper.getInstance().getFirebaseAnalytics().setAnalyticsUserId("123456");
```

## 衡量电子商务数据

参考官网 [衡量电子商务数据](https://firebase.google.com/docs/analytics/measure-ecommerce?hl=zh-cn)

## 衡量广告收入

参考官网 [衡量广告收入](https://firebase.google.com/docs/analytics/measure-ad-revenue?hl=zh-cn)

## 在 WebView 中使用 Analytics

参考官网 [在 WebView 中使用 Analytics](https://firebase.google.com/docs/analytics/webview?hl=zh-cn&platform=android)

## 使用 Cloud Functions 扩展 Google Analytics

参考官网 [Cloud Functions 扩展 Google Analytics](https://firebase.google.com/docs/analytics/extend-with-functions?hl=zh-cn)

## 配置 Analytics 数据收集和使用方式

参考官网 [配置 Analytics 数据收集和使用方式](https://firebase.google.com/docs/analytics/configure-data-collection?hl=zh-cn&platform=android)

如需重新启用收集功能（例如在最终用户提供同意后），请调用 setAnalyticsCollectionEnabled() 方法。例如：

**接口原型**

```java
    /**
     * 设置用户ID
     * @param id 用户ID
     */
    public void setAnalyticsUserId(String id);
```

**调用示例**

```java
    FirebaseSdkWrapper.getInstance().getFirebaseAnalytics().setAnalyticsCollectionEnabled(true);
```

如果您因任何原因需要再次暂停数据收集，可以调用

**调用示例**

```java
    FirebaseSdkWrapper.getInstance().getFirebaseAnalytics().setAnalyticsCollectionEnabled(false);
```

## Analytics 错误代码

参考官网 [Analytics 错误代码](https://firebase.google.com/docs/analytics/errors?hl=zh-cn)