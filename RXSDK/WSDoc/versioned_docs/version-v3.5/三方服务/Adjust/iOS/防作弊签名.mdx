---
title: "防作弊签名"
---

前往 [Adjust 官方文档](https://dev.adjust.com/zh/sdk/ios/integrations/signature-library?version=v4)。

Adjust 的 SDK 签名库是 Adjust SDK 的一款独立插件，也就是说，您无需变更代码或改变在应用中使用 Adjust SDK 的方式，即可使用 SDK 签名库。安装插件后，Adjust SDK 发出的所有请求都将继续如常运行。

**操作前须知**
要将 Adjust 签名库集成到您的 iOS 应用，请确保满足下列条件：

- 正在使用的 Adjust SDK 为 **4.35.2 或更高版本 。**
- 支持 iOS **12 或更高 版本。**

## SDK 集成

- 编辑 Podfile 文件，添加如下配置：

```objectivec
pod 'RXAdjustSigSDK'
```

## 进行应用测试
Adjust 对于测试环境配置有要求，需注意以下几点：
1. Adjust 要求测试测试环境必须使用 **sandbox** 模式，由于SDK 初始化时默认使用正式环境初始化，所以在进行 AdjustSign 防作弊签名测试时需要在SDK 初始化前调用 Adjust 初始化，参考调用示例：
```objectivec
// Adjust 初始化
RXADJConfig *config = [RXADJConfig configWithAppToken:@"此处替换为真实的 adjust token" environment:RXADJEnvironmentSandbox];
[[RXAdjust sharedSDK] appDidLaunch:config];
    
// SDK 初始化
[[RXService sharedSDK] initWithProductId:@"此处替换为真实的 productid"
                               channelId:@"此处替换为真实的 channelid"
                                    cpid:@"此处替换为真实的 cpid"
                             baseUrlList:@[此处替换为真实的 baseUrl]
                                complete:^(NSDictionary * _Nullable response, RX_CommonRequestError * _Nullable error) {
    if (!error) {
        NSLog(@"初始化成功");
    } else {
        NSLog(@"初始化失败");
    }
}];
```
2. 从测试设备中完全删除应用，确保安装信息能被正确发送，Adjust 归因触发条件为首次安装，每次测试都需要删除应用重新安装。
3. 使用测试控制台遗忘设备，清除与该设备相关的所有信息。
4. 将开发模式设置为 **release**，通过 xcode 直接运行会导致签名无效，可以通过以下方式进行调试： 
    - 将项目打包成 ipa 后安装 ipa；
    - 将 ipa 上传至 TestFlight 后通过 TestFlight 下载；
    - 将 ipa 上传至 蒲公英/fir.im 等平台生成二维码后扫码下载（部分平台对游戏项目管控较严格，可能上传失败）；
5. 应用启动后可在 Adjust 测试控制台根据**设备的广告 id（adid）**查看结果：
    - **签名验证结果** 为 **有效签名**；
    - **签名接收状态** 为 **已接收**；