---
title: "Android"
---

## SDK 集成

- 排行榜默认在base中已经集成，无需额外引入

## 增加用户分数

**接口原型**

```java
    /** 增加用户分数
     * @param rank_id  字符串本身带有类型信息，格式为：Flag_榜单容量_重置周期_自定义标识（flag是一个int64位标记，用以扩展和规定绑定属性）
     * @param score    增加的分数值
     * @param callback callback
     */
    void addScore(String rank_id, int score, RXRequestCallback callback);
```

### 参数说明

| **字段** | **类型** | **是否必填** | **备注**        |
| -------- | -------- | ------------ | --------------- |
| rank_id  | string   | 是           | 排行榜 ID       |
| score    | int      | 是           | 增加的分数值    |

**调用示例**

```json
    RuiXueSdk.getRXSdkApi().addScore(rank_id, score, jsonCallback);
```

### 响应结构

| **字段** | **类型** | **是否必填** | **说明** | **示例** |
| --- | --- | --- | --- | --- |
| code | Number | 是   | 响应状态码，0为成功，非0表示出错 | 0   |
| msg | String | 否   | 响应信息，仅在status为非0时不为空 |     |

## 设置用户分数

**接口原型**

```java
    /** 设置用户分数
     * @param rank_id  字符串本身带有类型信息，格式为：Flag_榜单容量_重置周期_自定义标识（flag是一个int64位标记，用以扩展和规定绑定属性）
     * @param score    增加的分数值
     * @param callback callback
     */
    void setScore(String rank_id, int score, RXRequestCallback callback);
```

### 参数说明

| **字段** | **类型** | **是否必填** | **备注**        |
| -------- | -------- | ------------ | --------------- |
| rank_id  | string   | 是           | 排行榜 ID       |
| score    | int      | 是           | 增加的分数值    |

**调用示例**

```json
    RuiXueSdk.getRXSdkApi().setScore(rank_id, score, jsonCallback);
```

### 响应结构

| **字段** | **类型** | **是否必填** | **说明** | **示例** |
| --- | --- | --- | --- | --- |
| code | Number | 是   | 响应状态码，0为成功，非0表示出错 | 0   |
| msg | String | 否   | 响应信息，仅在status为非0时不为空 |     |

## 查询用户分数

**接口原型**

```java
    /** 查询用户分数
     * @param open_id  目标用户 OpenID
     * @param callback callback
     */
    void queryUserRank(String rank_id, String open_id, RXRequestCallback callback);
```

### 参数说明

| **字段** | **类型** | **是否必填** | **备注**                                                                                                              |
| -------- | -------- | ------------ | --------------------------------------------------------------------------------------------------------------------- |
| rank_id  | string   | 是           | 字符串本身带有类型信息，格式为：Flag_榜单容量_重置周期_自定义标识（flag 是一个 int64 位标记，用以扩展和规定绑定属性） |
| open_id  | string   | 是           | 目标用户 OpenID                                                                                                       |

**调用示例**

```json
    RuiXueSdk.getRXSdkApi().queryUserRank(rank_id, open_id, jsonCallback);
```

### 响应结构

| **字段** | **类型**                       | **是否必填** | **说明** | **示例** |
| --- |------------------------------| --- | --- | --- |
| code | Number                       | 是   | 响应状态码，0为成功，非0表示出错 | 0   |
| msg | String                       | 否   | 响应信息，仅在status为非0时不为空 |     |
| data | [DataO](/docs/v3.5/排行榜/服务端接入/服务端 API) | 否   | 结果数据集 |     |

##### DataO

| **字段** | **类型**                       | **是否必填** | **说明** | **示例** |
| --- |------------------------------| --- | --- | --- |
| score | Number                       | 是   | 排行榜分数 |     |
| rank | Number                       | 是   | 排行榜排名 |     |
| user | [UserO](/docs/v3.5/排行榜/服务端接入/服务端 API) | 是   | 用户信息数据集 |     |

##### UserO

| **字段** | **类型** | **是否必填** | **说明** | **示例** |
| --- | --- | --- | --- | --- |
| openid | String | 是   | 用户望舒 OpenID |     |
| nickname | String | 是   | 用户昵称 |     |
| birthday | String | 否   | 用户生日日期 |     |
| remarks | String | 否   | 当前用户对其的备注 |     |
| avatar | String | 否   | 用户头像 |     |
| custom | String | 否   | CP 自定义信息 |     |
| time | Number | 否   | 关系形成时间戳（单位：秒） |     |
| sex | Number | 否   | 用户性别，1:男；0:女 |     |

## 获取排行榜列表

**接口原型**

```java
    /** 获取排行榜列表
     * @param rank_id    字符串本身带有类型信息，格式为：Flag_榜单容量_重置周期_自定义标识（flag是一个int64位标记，用以扩展和规定绑定属性）
     * @param start_rank 获取排行榜开始排名。取值[1,榜单容量)。可以用于分页加载	1
     * @param end_rank   获取排行榜结束排名。取值[1,榜单容量]。可以用于分页加载	2
     * @param callback   callback
     */
    void getRankList(String rank_id, int start_rank, int end_rank, RXRequestCallback callback);
```

### 参数说明

| **字段**   | **类型** | **是否必填** | **备注**                                                                                                              |
| ---------- | -------- | ------------ | --------------------------------------------------------------------------------------------------------------------- |
| rank_id    | string   | 是           | 字符串本身带有类型信息，格式为：Flag_榜单容量_重置周期_自定义标识（flag 是一个 int64 位标记，用以扩展和规定绑定属性） |
| start_rank | int      | 是           | 获取排行榜开始排名。取值[1,榜单容量)。可以用于分页加载                                                                |
| end_rank   | int      | 是           | 获取排行榜结束排名。取值[1,榜单容量]。可以用于分页加载                                                                |

**调用示例**

```json
    RuiXueSdk.getRXSdkApi().getRankList(rank_id, start_rank,end_rank, jsonCallback);
```

### 响应结构

| **字段** | **类型**                       | **是否必填** | **说明** | **示例** |
| --- |------------------------------| --- | --- | --- |
| code | Number                       | 是   | 响应状态码，0为成功，非0表示出错 | 0   |
| msg | String                       | 否   | 响应信息，仅在status为非0时不为空 |     |
| data | [][Node](/docs/v3.5/排行榜/服务端接入/服务端 API) | 否   | 结果 数组结构 |     |

##### Node

| **字段** | **类型**                       | **是否必填** | **说明** | **示例** |
| --- |------------------------------| --- | --- | --- |
| score | Number                       | 是   | 排行榜分数 |     |
| rank | Number                       | 是   | 排行榜排名 |     |
| user | [UserO](/docs/v3.5/排行榜/服务端接入/服务端 API) | 是   | 用户信息数据集 |     |

## 获取好友排行榜列表

- 接口原型

```java
    /** 获取好友排行榜列表
     * @param rank_id  字符串本身带有类型信息，格式为：Flag_榜单容量_重置周期_自定义标识（flag是一个int64位标记，用以扩展和规定绑定属性）
     * @param callback callback
     */
    void friendsRank(String rank_id, RXRequestCallback callback);
```

### 参数说明

| **字段** | **类型** | **是否必填** | **备注**                                                                                                              |
| -------- | -------- | ------------ | --------------------------------------------------------------------------------------------------------------------- |
| rank_id  | string   | 是           | 字符串本身带有类型信息，格式为：Flag_榜单容量_重置周期_自定义标识（flag 是一个 int64 位标记，用以扩展和规定绑定属性） |

**调用示例**

```json
    RuiXueSdk.getRXSdkApi().friendsRank(rank_id,jsonCallback);
```

### 响应结构

| **字段** | **类型**                               | **是否必填** | **说明** | **示例** |
| --- |--------------------------------------| --- | --- | --- |
| code | Number                               | 是   | 响应状态码，0为成功，非0表示出错 | 0   |
| msg | String                               | 否   | 响应信息，仅在status为非0时不为空 |     |
| data | [][ListNode](/docs/v3.5/排行榜/服务端接入/服务端 API) | 否   | 结果 数组结构 |     |

##### ListNode

| **字段** | **类型**                       | **是否必填** | **说明** | **示例** |
| --- |------------------------------| --- | --- | --- |
| score | Number                       | 是   | 排行榜分数 |     |
| rank | Number                       | 是   | 排行榜排名 |     |
| user | [UserO](/docs/v3.5/排行榜/服务端接入/服务端 API) | 是   | 用户信息数据集 |     |