---
description: 瑞雪SDK文档站 Docusaurus 编写与组织规范
alwaysApply: true
---

# 文档站通用规范

## MDX 共享组件

- 共享片段文件名以 `_` 开头（如 `_pay-common.mdx`、`_h5pay-common.mdx`），Docusaurus 会自动忽略不渲染
- 渠道专属页通过 `import PayCommon from './_pay-common.mdx'` 引入，只写差异内容
- 渠道特有的额外参数用 `:::info[标题]` Admonition 块补充，不重写整个参数表

## 支付模块分类原则

| SDK 依赖 | 接口 | 归属目录 |
| --- | --- | --- |
| `rxsdk_pay`（原生） | `RXSDK.getInstance().pay()` | `Android-渠道支付/` |
| `rxsdk_h5pay`（H5桥接） | `HQSdkWrapper.getInstance().doPay()` | `Android-H5支付/` |
| JS SDK（H5游戏端） | `sdk.pay()` JavaScript | 对应平台的 `三方服务/` |

- 判断依据：看 SDK 依赖，`rxsdk_h5pay` → `Android-H5支付/`
- 每个渠道文件必须包含：`瑞雪支付配置` 链接、`SDK 集成` 代码块、`hq_type 值` 表格

## 代码块语言标签

| 内容 | 标签 |
| --- | --- |
| Gradle 依赖 | ` ```groovy ` |
| Java 代码 | ` ```java ` |
| JavaScript/TypeScript | ` ```javascript ` / ` ```typescript ` |
| JSON | ` ```json ` |
| 纯文本/命令 | ` ```bash ` 或 ` ```text `，**禁止裸 ``` ** |

- Java 泛型必须完整：`Map<String, Object>`，不能写 `MapString, Object>`

## _category_.json 规范

- 单文件目录必须加 `"link": {"type": "doc", "id": "..."}` 避免额外点击
- `position` 字段控制同级排序，新增目录时要在同级内统一编号，不留冲突
- 子目录移入父目录后，`position` 要改为父目录内的相对序号（不再是全局值）

## 目录组织

- `docs/_archive/` 存放已废弃或被拆分替代的旧文件，不删除（保留 git 历史）
- `docusaurus.config.ts` 已配置 `exclude: ['_archive/**']`，归档内容不会出现在站点
- JS SDK 接入文档属于对应平台的 `三方服务/` 子目录，不放在 `支付/` 下

## 侧边栏（autogenerated）

- `支付` 分类使用 `autogenerated`，控制顺序只通过 `_category_.json` 的 `position`
- 新建渠道文件时 `sidebar_position` 从 1 开始，在同目录内唯一

### 多文档目录 + index 总览（`sidebars.ts` 手配分类）

适用于「可折叠分类 + 点击分类名进 index 总览 + 子项无重复 index 条目」（如 **更新日志**）：

1. **`sidebars.ts`**：外层 `category`，`link` 指向 `.../index`，`items` 内 `autogenerated` 子目录；保留 `collapsed` / `collapsible`
2. **`index.mdx`**：`sidebar_class_name: hidden` + `displayed_sidebar: tutorialSidebar`（侧栏隐藏重复条目，页面仍可访问）
3. **根 `_category_.json`**：只保留 `label`、`position`，**不要**再写 `link`（避免与 `sidebars.ts` 双层分类或重复条目）
4. **`src/css/custom.css`**：已有 `.menu__link.hidden` 隐藏规则，勿删

**禁止**：仅用顶层 `autogenerated` 替代手配分类（会丢失整块折叠）；也不要外层 category + `_category_.json` 双层同名 `link`（会出现两个「更新日志」）

子目录（如 `鸿蒙/`、`JavaScript/`）仍用各自 `_category_.json` 的 `link` + `index` 惯例即可。

## 海外渠道额外参数

海外支付渠道（如 `wch_hw`、`payermax`）需在 `:::info[海外/渠道特有参数]` 中补充：
- `user_real_currency`：用户实际支付币种
- `user_real_price`：用户实际支付金额
- 渠道特有的 `ext` 字段（如 PayerMax 的 `frontCallbackUrl`、`paymentMethod`）

## old_bak CSV 迁移

从 `old_bak/menu.csv`、`old_bak/article.csv` 回迁时：

- **仅导入 `use_status = 1`** 的菜单与文章（线上可见）
- **`use_status = 0`** 表示已删除/隐藏，**禁止**写入 docs
- 菜单链上任意祖先 `use_status = 0` 时，整支忽略
- 回迁脚本：`scripts/restore-from-old-bak.py`，过滤逻辑：`scripts/old_bak_filters.py`
- Tab 正文遗漏自检：`python3 scripts/old-bak-tab-audit.py --check`（对照 `article_tab_content`，报告见 `scripts/old-bak-tab-report.json`）
- `missing_tabs`：已映射 MDX 无 `<Tabs>` 且正文不足以覆盖 tab 内容；`content_without_tabs`：无 `<Tabs>` 但正文已足够（信息项，不导致 `--check` 失败）
- iOS/Android 分页合并：参考 `数据分析/上报设备通讯录.mdx`，单文件 + `<Tabs>`，旧路径加 `plugin-client-redirects` 重定向
