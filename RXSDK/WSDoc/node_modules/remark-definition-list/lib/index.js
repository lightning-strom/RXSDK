import { defListFromMarkdown, defListToMarkdown } from 'mdast-util-definition-list';
import { defList } from 'micromark-extension-definition-list';
export { defListHastHandlers } from 'mdast-util-definition-list';
export { defListHastToMdast } from 'hast-util-definition-list';
export function remarkDefinitionList() {
    var _a, _b, _c;
    const data = this.data();
    const micromarkExtensions = (_a = data.micromarkExtensions) !== null && _a !== void 0 ? _a : (data.micromarkExtensions = []);
    const fromMarkdownExtensions = (_b = data.fromMarkdownExtensions) !== null && _b !== void 0 ? _b : (data.fromMarkdownExtensions = []);
    const toMarkdownExtensions = (_c = data.toMarkdownExtensions) !== null && _c !== void 0 ? _c : (data.toMarkdownExtensions = []);
    micromarkExtensions.push(defList);
    fromMarkdownExtensions.push(defListFromMarkdown);
    toMarkdownExtensions.push(defListToMarkdown);
}
export default remarkDefinitionList;
