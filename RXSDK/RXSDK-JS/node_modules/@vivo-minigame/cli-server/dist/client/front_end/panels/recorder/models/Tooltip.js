import*as UI from"../../../ui/legacy/legacy.js";export function getTooltipForActions(t,o){let r=t;const c=UI.ShortcutRegistry.ShortcutRegistry.instance().shortcutsForAction(o);for(const t of c)r+=` - ${t.title()}`;return r}